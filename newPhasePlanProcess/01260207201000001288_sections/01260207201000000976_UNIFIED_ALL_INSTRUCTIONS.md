# UNIFIED MASTER TEMPLATE - ALL INSTRUCTIONS

**Version:** 1.0
**Generated:** 2026-01-28 12:47:09
**Total Sections:** 22
**Source Directory:** `C:\Users\richg\Gov_Reg\sections`

> **Note:** This file is auto-generated by `combine_sections.ps1`. Do not edit directly.
> Edit individual `sec_*.inst.md` files and re-run the script.

---

## Table of Contents

1. [SEC-000: Critical Constraints Instructions](#section-0)
2. [SEC-001: Plan Identity Instructions](#section-1)
3. [SEC-002: Task Analysis + Classification Instructions](#section-2)
4. [SEC-003: Executive Summary Instructions](#section-3)
5. [SEC-004: Assumptions & Scope Contract Instructions](#section-4)
6. [SEC-005: Phase DAG + Parallel Execution Instructions](#section-5)
7. [SEC-006: Ground Truth Validation Levels Instructions](#section-6)
8. [SEC-007: Self-Healing Rules + Circuit Breakers Instructions](#section-7)
9. [SEC-008: Worktree Isolation + Merge Strategy Instructions](#section-8)
10. [SEC-009: Environment + Tool Configuration Instructions](#section-9)
11. [SEC-010: Phase Contracts Instructions](#section-10)
12. [SEC-011: Artifact Manifest Instructions](#section-11)
13. [SEC-012: Global Validation Gates Instructions](#section-12)
14. [SEC-013: Risk + Unknown Management Instructions](#section-13)
15. [SEC-014: Decision Ledger Instructions](#section-14)
16. [SEC-015: Metrics + Observability Instructions](#section-15)
17. [SEC-016: Testing Strategy Instructions](#section-16)
18. [SEC-017: Governance + Anti-patterns Instructions](#section-17)
19. [SEC-018: GitHub Integration Instructions](#section-18)
20. [SEC-019: Operational Runbooks Instructions](#section-19)
21. [SEC-020: Final Summary + Completion Checklist Instructions](#section-20)
22. [SEC-021: Quality Controls Catalog](#section-21)

---


<a name="section-0"></a>

# SEC-000: Critical Constraints Instructions

**Schema File:** `sections/sec_000_critical_constraints.json`  
**Section ID:** `sec_000_critical_constraints`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Enforce the **NO IMPLIED BEHAVIOR** constraint and **DETERMINISM** constraint across the entire plan. This section defines non-negotiable rules that must be validated before plan execution.

---

## How To Do It

### Step 1: Understand the Constraints

**NO IMPLIED BEHAVIOR:**
- Every validation gate, rule, and contract MUST be expressed as:
  1. A config entry
  2. An executable command
  3. An evidence artifact path
  4. A pass/fail condition (regex or exit code)

**DETERMINISM:**
- Plans must achieve 95%+ reproducibility
- Same inputs → Same outputs across multiple runs

### Step 2: Mark Non-Automatable Items

If you encounter ANY item that cannot be expressed as config+command+evidence:
- Mark it with `[NOT_AUTOMATABLE]`
- Escalate with explicit human approval gate
- Document WHY it cannot be automated

### Step 3: Validate Against Forbidden Patterns

Scan the entire plan and flag these patterns:
- ❌ "works well", "good enough" (vague success criteria)
- ❌ "looks correct", "seems fine" (subjective pass/fail)
- ❌ "fix it" without specific action (open-ended)
- ❌ TODO, TBD, placeholder code in deliverables
- ❌ Skeleton/stub implementations in final output

### Step 4: Ensure All Gates Are Mechanical

For EVERY validation gate in the plan:
```
✓ Has executable command
✓ Has expected exit code
✓ Has regex pattern for stdout/stderr
✓ Has evidence output path
✓ Has timeout value
```

### Step 5: Validate Determinism Requirements

Ensure these exist elsewhere in the plan:
- Decision ledger with ALL decisions logged
- Assumption ledger with validation methods
- File scope enforcement (hard boundaries)
- Ground truth levels (L0-L5) with mechanical pass/fail
- DAG-scheduled parallel execution
- Bounded retry logic with convergence detection

---

## Validation Rules

1. **validation_command** must exist and be executable
2. **expect_exit_code** must be 0 for success validation
3. **expect_stdout_regex** must contain at least one pattern
4. **target_reproducibility** must be >= 0.95
5. All six determinism requirements must be satisfied by other sections

---

## Examples

### Example 1: Valid Mechanical Gate
```json
{
  "gate_id": "GATE-001",
  "command": "pytest tests/ -v --tb=short",
  "expect_exit_code": 0,
  "expect_stdout_regex": ["/PASSED/", "/100% coverage/"],
  "evidence_path": ".runs/test_evidence.log",
  "timeout_seconds": 300
}
```

### Example 2: Invalid (Vague) Gate
```json
{
  "gate_id": "GATE-BAD",
  "command": "manually review code quality",
  "expect": "looks good"
}
```
❌ This violates NO IMPLIED BEHAVIOR - must be marked [NOT_AUTOMATABLE] with justification.

---

## Common Mistakes

1. **Forgetting to mark manual steps** - Always add `[NOT_AUTOMATABLE]` tag
2. **Subjective success criteria** - "Good enough" is not mechanical
3. **Missing evidence paths** - Every command must write evidence
4. **No timeout** - All commands must have bounded execution time
5. **Implied behavior** - "Run tests" without specifying which tests/command

---

## AI Agent Notes

- This section is a **constraint enforcer**, not data collection
- Scan ALL other sections for violations
- Fail fast if violations found
- Generate validation report at: `.state/constraint_validation.json`



---


<a name="section-1"></a>

# SEC-001: Plan Identity Instructions

**Schema File:** `sections/sec_001_plan_identity.json`  
**Section ID:** `sec_001_plan_identity`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Fill in all plan metadata, ownership information, timestamps, scope, and fixed assumptions based on the user request.

---

## How To Do It

### Step 1: Generate `plan_id`

**Format:** `PLAN-{PROJECT_CODE}-{YYYYMMDD}-{NNN}`

1. Extract PROJECT_CODE from user request:
   - 2-15 characters
   - Uppercase letters, numbers, hyphens only
   - Example: "JSON validator" → "JSON-VALIDATOR"
   - Example: "Build API service" → "API-SERVICE"

2. Use current date (YYYYMMDD format): `20260128`

3. Start sequence number at 001 for first plan of the day

**Example:** `PLAN-JSON-VALIDATOR-20260128-001`

### Step 2: Extract `plan_title`

- Use first meaningful sentence from user request
- Remove filler words: "please", "can you", "I need", "help me"
- Maximum 100 characters
- Capitalize first letter only

**Examples:**
- Input: "Can you please build a CLI tool for JSON schema validation?"
- Output: "Build CLI tool for JSON schema validation"

### Step 3: Set `plan_version`

- New plans: `0.1.0`
- Revisions: increment patch version (0.1.1, 0.1.2, etc.)
- Major changes: increment minor version (0.2.0)

### Step 4: Set `plan_status`

Options: `Draft`, `Review`, `Approved`, `In Progress`, `Blocked`, `Done`

- Always start with: `Draft`
- Change when explicitly requested

### Step 5: Set `owner_dri`

- Use requesting user's email/username
- If not provided: `UNASSIGNED`
- DRI = Directly Responsible Individual

### Step 6: Set `reviewers`

- Leave empty `[]` unless explicitly mentioned
- If mentioned: `["user1@example.com", "user2@example.com"]`

### Step 7: Set Timestamps

- `created_date`: Current date (YYYY-MM-DD)
- `last_updated_date`: Current date (YYYY-MM-DD)
- `last_synced_at`: Leave as `null`

### Step 8: Determine `target_environments`

Infer from request or default to: `["windows", "linux", "macos"]`

Specific hints:
- "Windows only" → `["windows"]`
- "Docker" → `["docker"]`
- "AWS" / "cloud" → `["cloud"]`

### Step 9: Determine `execution_surface`

Options: `repo`, `service`, `library`, `infra`, `docs`, `mixed`

- Code files being modified → `repo`
- Deploying a service → `service`
- Building reusable component → `library`
- Infrastructure/config only → `infra`
- Documentation only → `docs`
- Multiple categories → `mixed`

### Step 10: Set `repo_or_workspace_root`

- Absolute path or URL
- If not provided, use: `{{PATH_OR_URL}}`
- Example: `C:\Users\richg\Gov_Reg`
- Example: `https://github.com/org/repo`

### Step 11: Set `primary_branch`

Default: `main`

Other options: `master`, `develop`

### Step 12: Add `related_documentation`

- Leave empty `[]` unless user references docs
- If mentioned: add path and type (spec|design|rfc|runbook)

### Step 13: Keep Fixed Assumptions

**DO NOT CHANGE** unless explicitly overridden:
- `team_model`: `solo_developer`
- `time_constraints`: `none`

---

## Validation Rules

1. `plan_id` MUST match pattern: `^PLAN-[A-Z0-9-]{2,15}-\d{8}-\d{3}$`
2. All dates MUST be YYYY-MM-DD format
3. `plan_version` MUST follow semantic versioning (X.Y.Z)
4. `repo_or_workspace_root` MUST be absolute path or valid URL
5. `primary_branch` MUST be one of: main, master, develop
6. `execution_surface` MUST be one of: repo, service, library, infra, docs, mixed

---

## Examples

### Example 1: Simple CLI Tool
**User request:** "Build a CLI tool to validate JSON schemas"

**Output:**
```json
{
  "metadata": {
    "plan_id": "PLAN-JSON-VALIDATOR-20260128-001",
    "plan_title": "Build CLI tool to validate JSON schemas",
    "plan_version": "0.1.0",
    "plan_status": "Draft",
    "file_id": "DOC-JSON-VALIDATOR-001"
  },
  "ownership": {
    "owner_dri": "richgwilks@gmail.com",
    "reviewers": []
  },
  "timestamps": {
    "created_date": "2026-01-28",
    "last_updated_date": "2026-01-28",
    "last_synced_at": null
  },
  "scope": {
    "target_environments": ["windows", "linux", "macos"],
    "execution_surface": "repo",
    "repo_or_workspace_root": "C:\\Users\\richg\\json-validator",
    "primary_branch": "main"
  }
}
```

### Example 2: Cloud Service
**User request:** "Deploy a REST API service to AWS using FastAPI"

**Output:**
```json
{
  "metadata": {
    "plan_id": "PLAN-FASTAPI-SERVICE-20260128-002",
    "plan_title": "Deploy REST API service to AWS using FastAPI",
    "plan_version": "0.1.0",
    "plan_status": "Draft",
    "file_id": "DOC-FASTAPI-SERVICE-001"
  },
  "scope": {
    "target_environments": ["cloud"],
    "execution_surface": "service",
    "repo_or_workspace_root": "https://github.com/myorg/fastapi-service",
    "primary_branch": "main"
  }
}
```

---

## Common Mistakes

1. **Using relative paths** - Always use absolute paths
2. **Forgetting to uppercase PROJECT_CODE** - Must be uppercase
3. **Including filler words in title** - Remove "please", "can you", etc.
4. **Changing fixed assumptions** - Leave team_model and time_constraints alone
5. **Invalid plan_id format** - Must follow exact pattern

---

## AI Agent Notes

- This section is ALWAYS the first to fill
- plan_id becomes the plan's primary identifier
- All other sections will reference this plan_id
- Keep it simple - don't overthink the extraction



---


<a name="section-2"></a>

# SEC-002: Task Analysis + Classification Instructions

**Schema File:** `sections/sec_002_task_analysis.json`  
**Section ID:** `sec_002_task_analysis`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

1. Capture the original user request verbatim
2. Transform vague requirements into **mechanically verifiable** targets
3. Classify the project complexity using the decision tree
4. Log the classification decision with rationale

---

## How To Do It

### Step 1: Capture Original Request

Copy the user's request **exactly as given**:
```json
{
  "original_request": {
    "verbatim": "Build a CLI tool to validate JSON schemas",
    "timestamp": "2026-01-28T06:09:17.948Z",
    "source": "user_input"
  }
}
```

**Source options:** `user_input`, `github_issue`, `slack_thread`, `other`

### Step 2: Transform Vague → Quantified

For EVERY vague requirement, create a quantified target with validation command:

**Template:**
```json
{
  "vague": "{{ORIGINAL_VAGUE_STATEMENT}}",
  "quantified": "{{MEASURABLE_TARGET}}",
  "validation": "{{EXECUTABLE_COMMAND}}",
  "expect_regex": "/{{PATTERN}}/"
}
```

**Common transformations:**

| Vague | Quantified | Validation Command |
|-------|------------|-------------------|
| "Fast" | "<100ms p95 latency" | `python benchmark.py \| grep p95` |
| "Reliable" | "100% tests pass" | `pytest --tb=short -q` |
| "Well-tested" | ">95% coverage" | `pytest --cov=src \| grep TOTAL` |
| "Secure" | "0 high/critical vulns" | `bandit -r src/ -ll` |
| "Clean code" | "0 lint errors" | `ruff check src/` |
| "Type-safe" | ">90% type coverage" | `mypy src/ --strict` |

**Create 5-10 quantified targets** covering:
- Performance (latency, throughput)
- Reliability (test pass rate, uptime)
- Quality (coverage, lint score, type coverage)
- Security (vulnerability scan)
- Maintainability (doc coverage, complexity metrics)

### Step 3: Run Classification Decision Tree

Analyze the request to count:
- `file_count`: Estimated number of files to create/modify
- `loc`: Estimated lines of code
- `requires_integration_tests`: true/false
- `requires_contracts`: true/false (multiple services/APIs)

**Apply decision tree rules in order:**

```
IF file_count <= 3 AND loc < 200
  → CLASSIFY-SIMPLE
  → verification_level: 1, test_coverage_min: 70%
  → phases: [intake, implement, verify]

ELSE IF file_count <= 10 AND loc < 1000
  → CLASSIFY-MODERATE
  → verification_level: 2, test_coverage_min: 80%
  → phases: [intake, plan, implement, verify]

ELSE IF file_count <= 30 OR requires_integration_tests
  → CLASSIFY-COMPLEX
  → verification_level: 2, test_coverage_min: 90%
  → phases: [intake, architecture, implement, integration, verify]

ELSE (file_count > 30 OR requires_contracts)
  → CLASSIFY-ENTERPRISE
  → verification_level: 3, test_coverage_min: 95%
  → phases: [bootstrap, architecture, contracts, implement_parallel, integration, hardening, package]
```

### Step 4: Set Classification Fields

Based on decision tree result:
```json
{
  "classification": {
    "complexity": "moderate",
    "domain": "technical",
    "quality": "standard",
    "inferred_behavior": {
      "verification_level": 2,
      "test_coverage_min": 80,
      "phases": ["intake", "plan", "implement", "verify"],
      "parallel_groups": []
    }
  }
}
```

**Domain options:**
- `technical` - Code/engineering work
- `analytical` - Data analysis/research
- `creative` - Design/content creation
- `general` - Mixed or unclear

**Quality options:**
- `standard` - Normal quality bar
- `high` - Elevated quality requirements
- `critical` - Safety/security critical
- `production` - Production-grade system

### Step 5: Log Classification Decision

Record the decision in SEC-014 (Decision Ledger):
```json
{
  "decision_id": "DEC-CLASSIFY-001",
  "category": "classification",
  "context": {
    "file_count": 8,
    "loc": 500,
    "requires_contracts": false,
    "requires_integration_tests": false
  },
  "options": ["simple", "moderate", "complex", "enterprise"],
  "selected_option": "moderate",
  "rationale": "File count (8) and LOC (500) fall within moderate range per decision tree rule CLASSIFY-MODERATE",
  "timestamp": "2026-01-28T06:09:17.948Z",
  "owner": "AI_AGENT"
}
```

---

## Validation Rules

1. `original_request.verbatim` MUST be non-empty
2. `timestamp` MUST be ISO 8601 format
3. MUST have 5-10 specificity transformations
4. Each transformation MUST have: vague, quantified, validation, expect_regex
5. `complexity` MUST be one of: simple, moderate, complex, enterprise
6. `verification_level` MUST match complexity (simple=1, moderate/complex=2, enterprise=3)
7. Decision ledger entry MUST be created

---

## Examples

### Example 1: CLI Tool (Moderate)
**Input:** "Build a CLI tool to validate JSON schemas"

**Analysis:**
- Estimated files: 6-8 (cli.py, validator.py, schema_loader.py, tests/)
- Estimated LOC: 400-600
- No integration tests needed
- No contracts needed

**Classification:** MODERATE
```json
{
  "classification": {
    "complexity": "moderate",
    "domain": "technical",
    "quality": "standard",
    "inferred_behavior": {
      "verification_level": 2,
      "test_coverage_min": 80,
      "phases": ["intake", "plan", "implement", "verify"],
      "parallel_groups": []
    }
  }
}
```

**Quantified targets:**
1. "Works correctly" → "100% of tests pass" | `pytest --tb=short -q` | exit_code=0
2. "Fast" → "<10ms validation time" | `time python validate.py sample.json` | /real.*0\.00[0-9]s/
3. "Well-tested" → ">80% coverage" | `pytest --cov=src` | /TOTAL.*8[0-9]|9[0-9]|100%/
4. "Clean code" → "0 lint errors" | `ruff check src/` | exit_code=0
5. "User-friendly" → "Help text exists" | `python cli.py --help` | /Usage:/

### Example 2: Microservice Architecture (Enterprise)
**Input:** "Build a microservices platform with 5 services, API gateway, and message queue"

**Analysis:**
- Estimated files: 50+ (multiple services)
- Estimated LOC: 5000+
- Requires integration tests (service interactions)
- Requires contracts (API contracts between services)

**Classification:** ENTERPRISE

---

## Common Mistakes

1. **Forgetting to quantify** - Every target must be measurable
2. **Missing validation commands** - Every target needs a command
3. **Wrong complexity level** - Follow decision tree strictly
4. **Skipping decision ledger** - Always log the classification decision
5. **Vague quantifications** - "Fast" is not quantified; "<100ms" is

---

## AI Agent Notes

- Classification drives the entire plan structure
- Higher complexity → more phases, gates, evidence
- Don't overthink file counts - estimate conservatively
- When in doubt between two levels, choose higher complexity



---


<a name="section-3"></a>

# SEC-003: Executive Summary Instructions

**Schema File:** `sections/sec_003_executive_summary.json`  
**Section ID:** `sec_003_executive_summary`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Create a decision-ready executive summary with:
1. One-sentence outcome statement
2. What changes vs. what doesn't change
3. Primary decisions needed (blocking questions)
4. Top 3 risks with mitigation
5. High-level "done" criteria

---

## How To Do It

### Step 1: Write One-Sentence Outcome

Synthesize the entire plan into ONE sentence:

**Template:** "Deliver {WHAT} that {KEY_CAPABILITY} with {MEASURABLE_OUTCOME}"

**Examples:**
- "Deliver CLI tool that validates JSON schemas with 100% test coverage and <10ms latency"
- "Deploy FastAPI service to AWS that handles 1000 req/sec with 99.9% uptime"
- "Refactor authentication module to support OAuth2 with zero downtime migration"

### Step 2: List What Changes

Be **specific and concrete**:
- ✅ "Create src/validator.py with schema validation logic"
- ✅ "Modify tests/test_api.py to add 15 new integration tests"
- ✅ "Update config/settings.yaml with new OAuth2 parameters"
- ❌ "Improve code quality" (too vague)
- ❌ "Refactor some files" (not specific)

Aim for 5-10 bullets.

### Step 3: List What Does NOT Change

Explicitly call out what's out of scope:
- "Database schema remains unchanged"
- "Existing REST API endpoints unmodified"
- "User authentication flow stays as-is"
- "No changes to deployment pipeline"

Aim for 3-5 bullets.

### Step 4: Identify Primary Decisions

Look for **blocking questions** that need answers before work can proceed:

**Template:**
```json
{
  "decision_id": "D1",
  "statement": "Which validation library to use?",
  "options": ["jsonschema", "pydantic", "custom"],
  "impact": "HIGH",
  "blocking": "YES"
}
```

**Impact levels:**
- HIGH: Affects multiple phases, changes architecture
- MEDIUM: Affects one phase, local scope
- LOW: Implementation detail, easily reversible

**Blocking:**
- YES: Cannot proceed without decision
- NO: Can defer decision to later phase

Only include decisions that are:
- Actually uncertain (not already decided)
- Need stakeholder input
- Block progress if not resolved

If no decisions needed, explicitly state: `[]` (empty array)

### Step 5: Identify Top 3 Risks

Use the risk register from SEC-013 but summarize the TOP 3 here:

**Template:**
```json
{
  "risk_id": "RISK-001",
  "type": "risk",
  "description": "External API might rate-limit requests",
  "impact": "MEDIUM",
  "probability": "HIGH",
  "mitigation": "Implement exponential backoff and request caching"
}
```

**Risk categories:**
- Technical: Dependencies, integration, performance
- Process: Timeline, resource availability
- External: Third-party APIs, vendor changes
- Resource: Skills, tools, budget

### Step 6: Define High-Level "Done"

Write 5 criteria that must ALL be true for plan completion:

**Examples:**
- "All 47 test cases pass in CI"
- "Code coverage ≥ 90%"
- "Security scan shows 0 high/critical issues"
- "Documentation updated with API examples"
- "Deployed to staging and smoke tests pass"

Must be:
- Measurable (not subjective)
- Binary (yes/no, not "mostly")
- Verifiable by command/script

---

## Validation Rules

1. `outcome_one_sentence` MUST be ≤ 150 characters
2. `what_changes` MUST have 5-10 items
3. `what_does_not_change` MUST have 3-5 items
4. Each decision MUST have: statement, options (≥2), impact, blocking
5. MUST have exactly 3 risks (or mark as RISK-NONE-001 if truly no risks)
6. `done_at_highest_level` MUST have exactly 5 criteria

---

## Examples

### Example 1: CLI Tool
```json
{
  "outcome_one_sentence": "Deliver CLI tool that validates JSON schemas with 95% test coverage and <10ms validation time",
  "what_changes": [
    "Create src/cli.py with argparse interface",
    "Create src/validator.py with jsonschema validation logic",
    "Create tests/test_validator.py with 20+ test cases",
    "Add requirements.txt with jsonschema>=4.0",
    "Create README.md with usage examples"
  ],
  "what_does_not_change": [
    "No existing code modified (greenfield project)",
    "No database or external services",
    "No CI/CD pipeline setup (manual execution)"
  ],
  "primary_decisions_needed": [
    {
      "decision_id": "D1",
      "statement": "Use jsonschema library or implement custom validator?",
      "options": ["jsonschema library", "custom implementation"],
      "impact": "MEDIUM",
      "blocking": "YES"
    }
  ],
  "primary_risks_top_3": [
    {
      "risk_id": "RISK-001",
      "description": "jsonschema library might not support draft-2020-12",
      "impact": "MEDIUM",
      "probability": "LOW",
      "mitigation": "Verify library version supports required draft, fallback to draft-07"
    },
    {
      "risk_id": "RISK-002",
      "description": "Large schemas (>1MB) might cause memory issues",
      "impact": "LOW",
      "probability": "MEDIUM",
      "mitigation": "Add file size validation and streaming parser"
    },
    {
      "risk_id": "RISK-003",
      "description": "User might provide malformed JSON files",
      "impact": "LOW",
      "probability": "HIGH",
      "mitigation": "Add try/except with friendly error messages"
    }
  ],
  "done_at_highest_level": [
    "All unit tests pass (exit code 0)",
    "Test coverage ≥ 95%",
    "CLI accepts --help and shows usage",
    "Validation completes in <10ms for 100KB schemas",
    "README.md contains 3+ usage examples"
  ]
}
```

---

## Common Mistakes

1. **Outcome too long** - Keep ≤ 150 chars, one sentence only
2. **Vague changes** - "Improve performance" not specific enough
3. **Fake decisions** - Don't list already-decided items
4. **Weak risk mitigation** - "Hope it doesn't happen" is not mitigation
5. **Subjective done criteria** - "Good test coverage" is not measurable

---

## AI Agent Notes

- This section is for **humans** to make go/no-go decisions
- Keep it high-level - details go in other sections
- Focus on what matters for decision-making
- If user already decided something, don't list as open decision



---


<a name="section-4"></a>

# SEC-004: Assumptions & Scope Contract Instructions

**Schema File:** `sections/sec_004_assumptions_scope.json`  
**Section ID:** `sec_004_assumptions_scope`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

1. Document ALL assumptions with validation methods
2. Define clear scope boundaries (in/out/non-goals)
3. State constraints (time, security, performance, regulatory)
4. Define global "definition of done" criteria

---

## How To Do It

### Step 1: Build Assumptions Ledger

For EVERY assumption in the plan, create an entry:

**Template:**
```json
{
  "assumption_id": "ASM-001",
  "statement": "Python 3.11+ is available on target systems",
  "impact_score": 8,
  "confidence": "HIGH",
  "validation_method": "Check Python version in CI",
  "validation_command": "python --version | grep 'Python 3.1[1-9]'",
  "expect_exit_code": 0,
  "expect_stdout_regex": ["/Python 3\\.1[1-9]/"],
  "not_automatable": false
}
```

**Impact score (1-10):**
- 9-10: Critical (plan fails if false)
- 7-8: High (major rework needed if false)
- 4-6: Medium (some rework needed if false)
- 1-3: Low (minor adjustments if false)

**Confidence:**
- HIGH: 90%+ certain assumption is true
- MEDIUM: 50-90% certain
- LOW: <50% certain (flag for validation)

**Common assumption categories:**
- Environment: OS, Python version, available tools
- Dependencies: Libraries, services, APIs available
- Data: File formats, schemas, data availability
- Access: Permissions, credentials, network access
- User behavior: Expected inputs, usage patterns

**Validation command MUST:**
- Be executable in CI/local environment
- Return 0 on success, non-zero on failure
- Write evidence to `.state/assumptions.jsonl`

**If truly not automatable:**
```json
{
  "assumption_id": "ASM-MANUAL-001",
  "statement": "Stakeholder approves UI design",
  "not_automatable": true,
  "validation_method": "[NOT_AUTOMATABLE] Requires human design review meeting"
}
```

Aim for **10-20 assumptions** - be thorough.

### Step 2: Define Scope Contract

#### In Scope (5-10 items)
Be **specific and concrete**:
- ✅ "Validate JSON files against JSON Schema draft-07"
- ✅ "Support schemas up to 10MB in size"
- ✅ "CLI interface with --file and --schema arguments"
- ❌ "Good validation" (vague)

#### Out of Scope (5-10 items)
Explicitly call out what's **excluded**:
- "No GUI/web interface"
- "No support for YAML schemas"
- "No schema generation from examples"
- "No cloud deployment"

#### Non-Goals (3-5 items)
Higher-level things we're **intentionally not solving**:
- "Not a general-purpose JSON parser"
- "Not a schema editor or IDE"
- "Not a schema registry service"

### Step 3: Define Constraints

Only fill if applicable:

```json
{
  "constraints": {
    "time_budget": "OPTIONAL - only if hard deadline exists",
    "security_compliance": "Must pass OWASP Top 10 checks",
    "performance_targets": "<100ms p95 latency",
    "regulatory": "GDPR compliant (no PII storage)"
  }
}
```

Leave as `{{OPTIONAL}}` if not applicable.

### Step 4: Define Global Definition of Done

Write **6-8 criteria** that apply to the ENTIRE plan:

**Standard criteria (always include):**
1. "Outputs are schema-valid (where schemas exist)"
2. "Deterministic outputs stable across 3 runs"
3. "Unit + integration tests pass (100%)"
4. "All 'must_pass' validations pass in every phase contract"
5. "Audit/evidence logs exist at declared locations"

**Add 1-3 project-specific criteria:**
- "CLI tool installed and runnable"
- "Documentation covers 80%+ of public APIs"
- "Security scan shows 0 high/critical issues"

---

## Validation Rules

1. MUST have ≥10 assumptions
2. Each assumption MUST have: statement, impact_score (1-10), validation_method
3. Automatable assumptions MUST have: validation_command, expect_exit_code, expect_stdout_regex
4. Not-automatable assumptions MUST have: not_automatable=true
5. In-scope MUST have 5-10 items
6. Out-of-scope MUST have 5-10 items
7. Global DoD MUST have 6-8 items

---

## Examples

### Example 1: High-Impact Assumption
```json
{
  "assumption_id": "ASM-001",
  "statement": "Target systems have Python 3.11 or higher",
  "impact_score": 9,
  "confidence": "HIGH",
  "validation_method": "Check Python version on CI and local",
  "validation_command": "python --version",
  "expect_exit_code": 0,
  "expect_stdout_regex": ["/Python 3\\.1[1-9]/"],
  "not_automatable": false
}
```

### Example 2: Not Automatable
```json
{
  "assumption_id": "ASM-005",
  "statement": "User has basic understanding of JSON Schema spec",
  "impact_score": 4,
  "confidence": "MEDIUM",
  "validation_method": "[NOT_AUTOMATABLE] Requires user documentation and examples",
  "not_automatable": true
}
```

### Example 3: Complete Scope Contract
```json
{
  "scope_contract": {
    "in_scope": [
      "Validate JSON files against JSON Schema draft-07 and draft-2020-12",
      "CLI interface with --file, --schema, --strict flags",
      "Support schemas up to 10MB in size",
      "Output validation errors in JSON format",
      "Exit code 0 for valid, 1 for invalid, 2 for errors"
    ],
    "out_of_scope": [
      "No GUI or web interface",
      "No YAML schema support",
      "No schema generation from JSON examples",
      "No schema editing capabilities",
      "No cloud/SaaS deployment"
    ],
    "non_goals": [
      "Not a general-purpose JSON parser library",
      "Not a schema registry or management system",
      "Not a schema documentation generator"
    ]
  }
}
```

---

## Common Mistakes

1. **Too few assumptions** - Capture EVERYTHING you're assuming
2. **Missing validation commands** - Every assumption needs verification
3. **Vague scope** - "Handle JSON" is not specific enough
4. **Forgetting "out of scope"** - Explicit exclusions prevent scope creep
5. **Weak DoD criteria** - "Good quality" is not measurable

---

## AI Agent Notes

- Assumptions are often **implicit** - make them explicit
- Higher impact scores → validate earlier in plan
- Out-of-scope is as important as in-scope
- This section prevents future "I thought you meant..." issues



---


<a name="section-5"></a>

# SEC-005: Phase DAG + Parallel Execution Instructions

**Schema File:** `sections/sec_005_phase_dag.json`  
**Section ID:** `sec_005_phase_dag`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Create a Directed Acyclic Graph (DAG) of all phases with:
1. Phase index (all phases listed)
2. Dependencies between phases
3. Parallel execution groups
4. Time estimates and bottlenecks

---

## How To Do It

### Step 1: List All Phases

Based on classification from SEC-002:
- **Simple:** intake, implement, verify
- **Moderate:** intake, plan, implement, verify
- **Complex:** intake, architecture, implement, integration, verify
- **Enterprise:** bootstrap, architecture, contracts, implement_parallel, integration, hardening, package

### Step 2: Define Dependencies

For each phase, specify:
- `depends_on`: HARD dependencies (must complete first)
- `may_run_parallel_with`: Safe to run concurrently
- `is_critical_path`: Affects total timeline

Example:
```json
{
  "phase_id": "PH-003",
  "depends_on": ["PH-001", "PH-002"],
  "may_run_parallel_with": ["PH-010"],
  "is_critical_path": true
}
```

### Step 3: Create Parallel Groups

Group phases that can run simultaneously:
```json
{
  "group_id": "PG-001",
  "group_name": "Implementation",
  "phases": ["PH-003", "PH-004", "PH-005"],
  "concurrency_limit": 3
}
```

### Step 4: Calculate Time Estimates

- Sequential: Sum all phase durations
- Parallel: Account for concurrent execution
- Speedup: (Sequential - Parallel) / Sequential * 100

---

## Validation Rules

1. No circular dependencies (DAG)
2. All phases have unique IDs
3. Dependencies reference valid phase IDs
4. At least one phase has no dependencies (entry point)
5. At least one phase has no dependents (exit point)

---

## AI Agent Notes

- Critical path determines minimum timeline
- Parallel execution can reduce total time 30-70%
- Bottlenecks are single-threaded phases on critical path



---


<a name="section-6"></a>

# SEC-006: Ground Truth Validation Levels Instructions

**Schema File:** `sections/sec_006_ground_truth_levels.json`  
**Section ID:** `sec_006_ground_truth_levels`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Define 6 validation levels (L0-L5) with executable commands and evidence paths.

---

## How To Do It

### Validation Levels

**L0: Syntax + Basic Validation**
- Files parse correctly
- Basic structure valid
- Commands: `python -m py_compile`, `yamllint`

**L1: Static Analysis**
- Lint checks pass
- Type checking passes
- Security scan clean
- Commands: `ruff check`, `mypy`, `bandit`

**L2: Unit Tests**
- All unit tests pass
- Coverage target met (70-95% based on classification)
- Commands: `pytest tests/unit --cov=src`

**L3: Integration Tests**
- Component interactions verified
- Commands: `pytest tests/integration`

**L4: End-to-End Tests**
- Full workflows verified
- Commands: `pytest tests/e2e`

**L5: Production Validation**
- Smoke tests in prod-like environment
- Commands: `docker-compose up`, smoke tests

### For Each Level, Specify:

```json
{
  "level": "L2",
  "name": "Unit Tests",
  "commands": [{
    "command": "pytest tests/unit -v --cov=src",
    "timeout_seconds": 300,
    "expect_exit_code": 0,
    "expect_stdout_regex": ["/PASSED/", "/TOTAL.*9[5-9]|100%/"]
  }],
  "evidence_path": ".runs/validation/L2_unit_tests.log",
  "required_for_phases": ["implement", "refactor"]
}
```

### Map Phases to Minimum Levels

- Intake: L0
- Implement: L2
- Integration: L3
- Hardening/Package: L4-L5

---

## Validation Rules

1. All 6 levels defined (L0-L5)
2. Each level has ≥1 command
3. All commands have timeout and evidence path
4. Per-phase requirements map phases to levels

---

## AI Agent Notes

- Higher levels include lower levels (L3 implies L0, L1, L2 pass)
- Classification determines minimum required level
- Evidence paths must be under `.runs/validation/`



---


<a name="section-7"></a>

# SEC-007: Self-Healing Rules + Circuit Breakers Instructions

**Schema File:** `sections/sec_007_self_healing.json`  
**Section ID:** `sec_007_self_healing`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Define bounded retry logic and circuit breakers for common failure modes.

---

## How To Do It

### Step 1: Define Failure Taxonomy

For each common failure type:

```json
{
  "failure_type": "TEST_FAIL",
  "allowed_actions": [
    "rerun tests with --lf (last failed)",
    "apply deterministic fix pattern from test output",
    "skip flaky test with waiver justification"
  ],
  "max_retry_budget": 2,
  "abort_conditions": [{
    "regex": "/FAILED.*AssertionError/i",
    "reason": "Logic error (not transient)"
  }],
  "evidence_per_attempt": ".runs/fix_loop/test_fail_attempt_{{N}}.log"
}
```

### Common Failure Types

1. **DEP_INSTALL_FAIL** - Dependency installation failures
   - Actions: retry with --force-reinstall, clear cache, use mirror
   - Max retries: 2

2. **TEST_FAIL** - Test execution failures
   - Actions: rerun with --lf, apply fix pattern
   - Max retries: 2

3. **LINT_FAIL** - Linter failures
   - Actions: run formatter, apply auto-fixes
   - Max retries: 1

4. **TYPE_CHECK_FAIL** - Type checking failures
   - Actions: add annotations, add type: ignore
   - Max retries: 2

5. **INTEGRATION_TEST_FAIL** - Integration test failures
   - Actions: verify service availability, update mocks
   - Max retries: 2

### Step 2: Configure Convergence Detection

```json
{
  "convergence_detection": {
    "enabled": true,
    "window_size": 3,
    "stability_threshold": 0.9,
    "max_total_attempts_per_phase": 5
  }
}
```

### Step 3: Configure Circuit Breakers

```json
{
  "circuit_breakers": {
    "enabled": true,
    "oscillation_window": 3,
    "oscillation_threshold": 2,
    "on_breaker_trip": "mark_phase_failed"
  }
}
```

---

## Validation Rules

1. ≥5 failure types defined
2. Each has allowed_actions, max_retry_budget, abort_conditions
3. Convergence detection configured
4. Circuit breakers configured

---

## AI Agent Notes

- Self-healing is BOUNDED - max retries prevent infinite loops
- Circuit breakers detect oscillation (same error repeatedly)
- Evidence logged at every attempt for debugging



---


<a name="section-8"></a>

# SEC-008: Worktree Isolation + Merge Strategy Instructions

**Schema File:** `sections/sec_008_worktree_isolation.json`  
**Section ID:** `sec_008_worktree_isolation`  
**Required:** No (Optional)  
**Updated:** 2026-01-28

---

## What To Do

Configure git worktree isolation for parallel phase execution (optional for simple projects).

---

## How To Do It

### Step 1: Decide If Worktree Isolation Needed

**Enable if:**
- Multiple phases run in parallel
- Phases touch different file sets
- Need isolation to prevent conflicts

**Skip if:**
- Simple/moderate project (sequential phases)
- Single developer, no parallelization

### Step 2: Configure Strategy

```json
{
  "enabled": true,
  "strategy": {
    "mode": "per_workstream",
    "base_branch": "main",
    "max_parallel_workers": 3,
    "naming_patterns": {
      "worktree_dir": ".worktrees/wt-${workstream_id}-${phase_id}",
      "branch_name": "feature/${phase_id}-${workstream_id}"
    }
  }
}
```

### Step 3: Define Merge Strategy

```json
{
  "merge_strategy": {
    "method": "squash-merge",
    "conflict_policy": "abort_and_escalate",
    "pre_merge_validations": [
      "All tests pass in worktree",
      "No scope violations detected"
    ]
  }
}
```

---

## Validation Rules

1. If enabled=true, all strategy fields required
2. max_parallel_workers ≤ CPU cores
3. merge_strategy must be specified

---

## AI Agent Notes

- Most projects can skip this (set enabled=false)
- Useful for enterprise projects with parallel workstreams
- Each worktree is isolated - no cross-contamination



---


<a name="section-9"></a>

# SEC-009: Environment + Tool Configuration Instructions

**Schema File:** `sections/sec_009_environment_tools.json`  
**Section ID:** `sec_009_environment_tools`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Define complete environment requirements, dependencies, and tool configuration.

---

## How To Do It

### Step 1: Set Target Environment

```json
{
  "target_os": ["windows", "linux", "macos"],
  "default_shell": "powershell",
  "primary_language": "python"
}
```

### Step 2: Configure Language Settings

```json
{
  "language_config": {
    "python": {
      "version_constraint": ">=3.11,<3.13",
      "venv_path": ".venv",
      "package_manager": "pip"
    }
  }
}
```

### Step 3: List Required Services

```json
{
  "required_services": [{
    "name": "sqlite_db",
    "type": "file",
    "path": ".state/pipeline.db",
    "validation_command": "sqlite3 .state/pipeline.db 'SELECT 1;'",
    "expect_exit_code": 0
  }]
}
```

### Step 4: List Dependencies

For each dependency:
```json
{
  "name": "pytest",
  "version": ">=7.4",
  "purpose": "unit/integration testing",
  "dev_only": true
}
```

**Standard dependencies:**
- pytest, pytest-cov (testing)
- ruff (linting)
- mypy (type checking)
- pyyaml, jsonschema (parsing)
- click (CLI)
- requests (HTTP)
- psutil (metrics)
- rich (output formatting)

### Step 5: Define Tool Profiles

```json
{
  "tool_profiles": {
    "preferred_ids": ["pytest", "ruff", "mypy"],
    "tool_definitions": [{
      "tool_id": "pytest",
      "executable": "pytest",
      "version_constraint": ">=7.4",
      "purpose": "unit/integration testing"
    }]
  }
}
```

### Step 6: Set Resource Requirements

```json
{
  "resource_requirements": {
    "minimum": {"ram_gb": 8, "disk_gb": 10, "cpu_cores": 2},
    "recommended": {"ram_gb": 16, "disk_gb": 50, "cpu_cores": 4}
  }
}
```

---

## Validation Rules

1. primary_language must be specified
2. ≥5 dependencies listed
3. All dependencies have: name, version, purpose
4. Resource requirements defined
5. Tool profiles list ≥3 tools

---

## AI Agent Notes

- This section defines reproducible environment
- All dependencies must be installable via package manager
- Version constraints prevent "works on my machine" issues



---


<a name="section-10"></a>

# SEC-010: Phase Contracts Instructions

**Schema File:** `sections/sec_010_phase_contracts.json`  
**Section ID:** `sec_010_phase_contracts`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Create a machine-checkable contract for EVERY phase in the plan.

---

## How To Do It

### Phase Contract Structure

Each phase contract MUST have:

1. **phase** - Identity and metadata
2. **dependencies** - Which phases must complete first
3. **scope** - Path policy (allowed/forbidden globs)
4. **tasks** - Atomic work items
5. **artifacts** - Files created/modified/deleted
6. **validations** - Commands that prove correctness
7. **completion_gate** - Rules that must pass
8. **extensions** - Risks, questions, entry criteria

### Step 1: Define Phase Identity

```json
{
  "phase": {
    "phase_id": "PH-001",
    "workstream_id": "WS-001",
    "title": "Bootstrap Environment",
    "summary": "Set up Python environment and install dependencies",
    "objective": "Python 3.11 venv created with all dependencies installed",
    "phase_type": "implementation",
    "status": "planned"
  }
}
```

### Step 2: Define Scope with Path Policy

```json
{
  "scope": {
    "path_policy": {
      "allowed": [{
        "rule_id": "PATH-ALLOW-001",
        "globs": ["requirements.txt", ".venv/**"],
        "modes": ["read", "create", "modify"]
      }],
      "forbidden": [{
        "rule_id": "PATH-DENY-001",
        "globs": ["src/**", "tests/**"],
        "modes": ["create", "modify", "delete"]
      }]
    }
  }
}
```

**CRITICAL:** Every file touch MUST be in `allowed` globs.

### Step 3: Define Tasks

```json
{
  "tasks": [{
    "task_id": "TASK-001",
    "title": "Create virtual environment",
    "description": "Create Python 3.11 venv at .venv/",
    "acceptance_criteria": [
      ".venv/ directory exists",
      "python interpreter is 3.11+"
    ],
    "artifact_ids_touched": ["ART-001"],
    "validation_ids": ["VAL-001"]
  }]
}
```

### Step 4: Define Artifacts

```json
{
  "artifacts": [{
    "artifact_id": "ART-001",
    "path": ".venv/bin/python",
    "action": "create",
    "classification": "config",
    "owner": {"phase_id": "PH-001", "task_id": "TASK-001"},
    "must_exist_after_phase": true
  }]
}
```

**Actions:** read, create, modify, delete

### Step 5: Define Validations

```json
{
  "validations": [{
    "validation_id": "VAL-001",
    "kind": "command",
    "description": "Verify Python 3.11+ installed",
    "command": ".venv/bin/python --version",
    "timeout_seconds": 10,
    "success_criteria": {
      "exit_code": 0,
      "stdout_patterns_all": ["/Python 3\\.1[1-9]/"]
    },
    "evidence_output": {
      "log_path": ".runs/PH-001/val_001.log"
    },
    "must_pass": true
  }]
}
```

### Step 6: Define Completion Gate

```json
{
  "completion_gate": {
    "rules": {
      "dependencies_satisfied": true,
      "all_required_artifacts_present": true,
      "no_scope_violations": true,
      "all_must_pass_validations_passed": true
    }
  }
}
```

---

## Validation Rules

1. Every task references only existing artifact_ids and validation_ids
2. scope.path_policy has ≥1 allowed AND ≥1 forbidden rule
3. All artifacts have: artifact_id, path, action, owner
4. All validations have: command, timeout, success_criteria, evidence_path
5. completion_gate.rules fully specified

---

## AI Agent Notes

- This is the MOST IMPORTANT section
- Phase contracts are the "source of truth" for execution
- No implied behavior - everything explicit
- One contract per phase (5-15 contracts typical)



---


<a name="section-11"></a>

# SEC-011: Artifact Manifest Instructions

**Schema File:** `sections/sec_011_artifact_manifest.json`  
**Section ID:** `sec_011_artifact_manifest`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Create cross-phase "bill of materials" for ALL files touched across the plan.

---

## How To Do It

### Step 1: Aggregate Artifacts from Phase Contracts

Extract ALL artifacts from SEC-010 phase contracts and consolidate into one table.

### Step 2: Create Manifest Table

For each artifact:
```json
{
  "phase_id": "PH-001",
  "artifact_id": "ART-001",
  "action": "create",
  "path": "src/pipeline/state.py",
  "introduced_in_phase": "PH-001",
  "purpose": "State machine implementation",
  "validation_ids": ["VAL-001", "VAL-002"],
  "validation_command_hint": "pytest tests/test_state.py"
}
```

### Step 3: Verify Consistency

Run consistency check:
```bash
python scripts/validate_artifact_manifest.py {{PLAN_FILE}}
```

**Checks:**
- All artifact_ids from phase contracts appear in manifest
- All paths are unique within action type
- No artifacts reference non-existent validation_ids
- introduced_in_phase matches first appearance

---

## Validation Rules

1. manifest_table.entries has ALL artifacts from ALL phase contracts
2. Each entry has: phase_id, artifact_id, action, path, purpose
3. No duplicate artifact_ids
4. All validation_ids reference valid validations
5. Consistency check command defined

---

## AI Agent Notes

- This is DERIVED from SEC-010 (phase contracts)
- Single source of truth for "what files will be touched"
- Used by scope violation detection
- Must be kept in sync with phase contracts



---


<a name="section-12"></a>

# SEC-012: Global Validation Gates Instructions

**Schema File:** `sections/sec_012_validation_gates.json`  
**Section ID:** `sec_012_validation_gates`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Define global validation gates that apply across all phases (in addition to phase-specific validations).

---

## How To Do It

### Standard Global Gates

Define these 4 gates (minimum):

**1. GATE-STRUCTURE-001: Directory Structure**
```json
{
  "gate_id": "GATE-STRUCTURE-001",
  "name": "Directory Structure Validation",
  "command": "python scripts/validate_structure.py --schema schemas/dir_structure.json",
  "timeout_seconds": 10,
  "success_criteria": {
    "exit_code": 0,
    "expect_stdout_regex": ["/all_required_dirs_present: true/"]
  },
  "evidence_path": ".runs/global_gates/structure.log",
  "must_pass": true,
  "run_after_phase": null
}
```

**2. GATE-COVERAGE-001: Global Coverage**
```json
{
  "gate_id": "GATE-COVERAGE-001",
  "name": "Global Coverage Threshold",
  "command": "pytest --cov=src --cov-report=term | grep TOTAL",
  "timeout_seconds": 300,
  "success_criteria": {
    "exit_code": 0,
    "expect_stdout_regex": ["/TOTAL.*9[5-9]|100%/"]
  },
  "must_pass": true,
  "run_after_phase": "PH-IMPLEMENT"
}
```

**3. GATE-SECURITY-001: Security Scan**
```json
{
  "gate_id": "GATE-SECURITY-001",
  "name": "Security Scan",
  "command": "bandit -r src/ -f json -o .runs/global_gates/security.json -ll",
  "timeout_seconds": 120,
  "success_criteria": {
    "exit_code": 0,
    "file_content_assertions": [{
      "path": ".runs/global_gates/security.json",
      "json_path": "$.results",
      "expect_value": "[]"
    }]
  },
  "must_pass": true,
  "run_after_phase": "PH-HARDENING"
}
```

**4. GATE-DETERMINISM-001: Determinism Check**
```json
{
  "gate_id": "GATE-DETERMINISM-001",
  "name": "Determinism Verification",
  "command": "python scripts/test_determinism.py --runs 3 --compare-outputs",
  "timeout_seconds": 600,
  "success_criteria": {
    "exit_code": 0,
    "expect_stdout_regex": ["/determinism_score: (0\\.[9][5-9]|1\\.0)/"]
  },
  "must_pass": true,
  "run_after_phase": "PH-PACKAGE"
}
```

### Gate Execution Policy

```json
{
  "gate_execution_policy": {
    "run_all_after_each_phase": false,
    "run_specific_after_phases": true,
    "fail_fast": true,
    "parallel_execution": false
  }
}
```

---

## Validation Rules

1. ≥4 global gates defined
2. Each gate has: gate_id, command, timeout, success_criteria, evidence_path
3. All gate_ids follow pattern: GATE-{NAME}-{NNN}
4. All evidence paths under `.runs/global_gates/`
5. Gate execution policy specified

---

## AI Agent Notes

- Global gates supplement phase-specific validations
- Run at specific points (after certain phases complete)
- Fail fast prevents wasted work downstream



---


<a name="section-13"></a>

# SEC-013: Risk + Unknown Management Instructions

**Schema File:** `sections/sec_013_risk_unknown_management.json`  
**Section ID:** `sec_013_risk_unknown_management`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Track risks, unknowns, and open questions across the plan.

---

## How To Do It

### Step 1: Build Risk Register

For each identified risk:
```json
{
  "risk_id": "RISK-001",
  "type": "risk",
  "category": "technical",
  "description": "External API might rate-limit requests",
  "impact": "MEDIUM",
  "probability": "HIGH",
  "risk_score": 7,
  "mitigation": "Implement exponential backoff and request caching",
  "fallback": "Switch to alternative API provider",
  "detection_signal": "HTTP 429 responses in logs",
  "owner": "{{DRI}}",
  "status": "open"
}
```

**Categories:** technical, process, resource, external

**Risk score:** impact (1-5) × probability (1-2) = 1-10

Aim for **10-15 risks** - be thorough.

### Step 2: Build Unknown Register

For genuine uncertainties:
```json
{
  "unknown_id": "UNK-001",
  "type": "unknown",
  "category": "technical",
  "description": "Unclear if library supports draft-2020-12 schemas",
  "impact": "HIGH",
  "investigation_plan": "Test with sample draft-2020-12 schema",
  "investigation_budget": "2 hours",
  "fallback": "Use draft-07 schemas instead",
  "owner": "{{DRI}}",
  "status": "open"
}
```

**Categories:** technical, requirements, data, integration

### Step 3: Capture Open Questions

For questions needing answers:
```json
{
  "question_id": "Q-001",
  "question": "Should CLI output JSON or human-readable text by default?",
  "why_it_matters": "Affects CLI interface design and testing",
  "options": ["JSON", "human-readable", "both with --format flag"],
  "resolution_step": "Ask user for preference",
  "blocking": "YES",
  "owner": "{{DRI}}",
  "status": "open"
}
```

**Blocking:**
- YES: Cannot proceed without answer
- NO: Can defer to later phase

---

## Validation Rules

1. ≥5 risks in register
2. Each risk has: description, impact, probability, mitigation, fallback
3. Each unknown has: description, investigation_plan, fallback
4. Each question has: question, options, resolution_step, blocking

---

## AI Agent Notes

- Risks are "known unknowns" (we know what might go wrong)
- Unknowns are "unknown unknowns" (genuine uncertainty)
- Questions are decision points needing stakeholder input
- Update status as risks are mitigated / unknowns resolved



---


<a name="section-14"></a>

# SEC-014: Decision Ledger Instructions

**Schema File:** `sections/sec_014_decision_ledger.json`  
**Section ID:** `sec_014_decision_ledger`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Create append-only log of ALL decisions made during planning.

---

## How To Do It

### For Each Decision, Record:

```json
{
  "decision_id": "DEC-PROJECT-001",
  "category": "tooling",
  "timestamp": "2026-01-28T06:09:17.948Z",
  "context": {
    "problem": "Need JSON schema validation library",
    "constraints": ["Must support draft-07 and draft-2020-12", "Active maintenance"]
  },
  "options": ["jsonschema", "pydantic", "custom implementation"],
  "selected_option": "jsonschema",
  "rationale": "jsonschema library has best draft-2020-12 support, actively maintained, 50M+ downloads/month",
  "owner": "AI_AGENT",
  "impact": "MEDIUM",
  "reversible": "YES",
  "validation": {
    "command": "pip show jsonschema | grep Version",
    "expect_exit_code": 0
  }
}
```

### Decision Categories

- **classification** - Project complexity level
- **architecture** - System design choices
- **tooling** - Tool/library selections
- **process** - Workflow decisions
- **technical** - Implementation choices
- **scope** - In/out of scope decisions
- **quality** - Quality gate selections

### When to Log a Decision

Log EVERY decision where you chose between alternatives:
- Library/tool selection
- Design pattern choice
- File structure
- Testing approach
- Validation method
- Phase ordering

**Don't log:**
- Obvious/default choices with no alternatives
- Mechanical derivations from other data

**Target:** 10-20 decisions for a moderate project

---

## Validation Rules

1. ≥5 decisions logged
2. Each has: decision_id, category, options (≥2), selected_option, rationale
3. All decision_ids unique and follow pattern: DEC-{PROJECT}-{NNN}
4. All timestamps in ISO 8601 format
5. Validation command defined if decision is verifiable

---

## AI Agent Notes

- Decision ledger enables determinism and auditability
- Record WHY you chose option A over B (rationale is key)
- Future AI agents can understand past choices
- Append-only = historical record preserved



---


<a name="section-15"></a>

# SEC-015: Metrics + Observability Instructions

**Schema File:** `sections/sec_015_metrics_observability.json`  
**Section ID:** `sec_015_metrics_observability`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Define quantified metrics to track and observability configuration.

---

## How To Do It

### Step 1: Define Tracked Metrics

**Standard metrics (always include):**

1. **MET-001: test_coverage_percentage**
   - Target: 95% (or per classification)
   - Command: `pytest --cov=src --cov-report=json:.runs/coverage.json && jq '.totals.percent_covered' .runs/coverage.json`

2. **MET-002: lint_errors**
   - Target: 0
   - Command: `ruff check src/ --output-format json | jq 'length'`

3. **MET-003: type_coverage_percentage**
   - Target: 90%
   - Command: `mypy src/ --txt-report .runs/mypy-report`

4. **MET-004: build_time_seconds**
   - Target: ≤60
   - Command: `time python -m build`

5. **MET-005: test_execution_time_seconds**
   - Target: ≤300
   - Command: `pytest --durations=0 -q`

6. **MET-006: security_issues_high_critical**
   - Target: 0
   - Command: `bandit -r src/ -f json | jq '[.results[] | select(.issue_severity | IN("HIGH", "CRITICAL"))] | length'`

7. **MET-007: documentation_coverage_percentage**
   - Target: 80%
   - Command: `python scripts/check_doc_coverage.py src/`

8. **MET-008: determinism_score**
   - Target: 0.95
   - Command: `python scripts/test_determinism.py --runs 3`

### Metric Entry Format

```json
{
  "metric_id": "MET-001",
  "metric_name": "test_coverage_percentage",
  "target_value": 95,
  "target_operator": ">=",
  "current_value": null,
  "unit": "percent",
  "collection_command": "{{COMMAND}}"
}
```

### Step 2: Configure Storage

```json
{
  "storage": {
    "format": "jsonl",
    "path": ".state/metrics.jsonl",
    "rotation_policy": "size_based",
    "max_size_mb": 100,
    "keep_files": 10
  }
}
```

### Step 3: Configure Event Logging

```json
{
  "event_logging": {
    "enabled": true,
    "format": "jsonl",
    "path": ".state/events.jsonl",
    "event_tags": [
      "phase:${phase_id}",
      "workstream:${workstream_id}"
    ]
  }
}
```

---

## Validation Rules

1. ≥8 metrics defined
2. Each metric has: metric_id, metric_name, target_value, target_operator, collection_command
3. Storage configuration specified
4. Event logging configured

---

## AI Agent Notes

- Metrics provide quantified progress tracking
- All metrics must be machine-collectable
- Targets based on classification level (simple=70%, enterprise=95%)
- JSONL format enables append-only metric history



---


<a name="section-16"></a>

# SEC-016: Testing Strategy Instructions

**Schema File:** `sections/sec_016_testing_strategy.json`  
**Section ID:** `sec_016_testing_strategy`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Define test-first development approach with test layers, fixtures, and quality gates.

---

## How To Do It

### Step 1: Enable Test-First Development

```json
{
  "test_first_development": {
    "enabled": true,
    "description": "Write tests alongside code (TDD)",
    "requirement": "Tests must pass 100% (or explicit exception with mitigation)"
  }
}
```

### Step 2: Define Test Layers

**Unit Tests:**
```json
{
  "unit": {
    "description": "Test individual functions/classes in isolation",
    "coverage_target": "95%",
    "location": "tests/unit/**",
    "run_command": "pytest tests/unit -v --tb=short --cov=src",
    "timeout_seconds": 300,
    "quality_gates": [
      "0 failures",
      "Coverage >= target",
      "No skipped tests without justification"
    ]
  }
}
```

**Integration Tests:**
```json
{
  "integration": {
    "description": "Test component interactions",
    "coverage_target": "80%",
    "location": "tests/integration/**",
    "run_command": "pytest tests/integration -v --tb=short",
    "timeout_seconds": 600,
    "quality_gates": [
      "0 failures",
      "All critical paths tested"
    ]
  }
}
```

**E2E Tests:**
```json
{
  "e2e": {
    "description": "Test complete workflows end-to-end",
    "coverage_target": "70%",
    "location": "tests/e2e/**",
    "run_command": "pytest tests/e2e -v --tb=short",
    "timeout_seconds": 1800,
    "quality_gates": [
      "0 failures",
      "All user stories covered"
    ]
  }
}
```

**Determinism Tests:**
```json
{
  "determinism": {
    "description": "Verify outputs are stable across runs",
    "location": "tests/determinism/**",
    "run_command": "python scripts/test_determinism.py --runs 3",
    "timeout_seconds": 600,
    "quality_gates": [
      "Determinism score >= 0.95"
    ]
  }
}
```

### Step 3: Define Test Fixtures

```json
{
  "test_fixtures": {
    "valid_inputs": {
      "location": "tests/fixtures/valid/**",
      "description": "Sample valid input data",
      "count": 10
    },
    "invalid_inputs": {
      "location": "tests/fixtures/invalid/**",
      "description": "Sample invalid input data with expected errors",
      "count": 10
    },
    "expected_outputs": {
      "location": "tests/fixtures/expected/**",
      "description": "Expected outputs for each input",
      "format": "json"
    }
  }
}
```

### Step 4: Set Quality Gates

```json
{
  "quality_gates_summary": {
    "run_all_tests_command": "pytest tests/ -v --tb=short --cov=src",
    "expect_exit_code": 0,
    "gates": [
      "100% of tests pass",
      "Coverage >= classification-based target",
      "Determinism score >= 0.95"
    ]
  }
}
```

---

## Validation Rules

1. All 4 test layers defined (unit, integration, e2e, determinism)
2. Each layer has: location, run_command, timeout, quality_gates
3. Test fixtures defined for valid/invalid inputs
4. Quality gates summary specified

---

## AI Agent Notes

- Tests are written WITH code, not after
- Coverage targets based on classification (simple=70%, enterprise=95%)
- Determinism tests verify reproducibility
- 100% pass rate is non-negotiable



---


<a name="section-17"></a>

# SEC-017: Governance + Anti-patterns Instructions

**Schema File:** `sections/sec_017_governance.json`  
**Section ID:** `sec_017_governance`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Define blocked anti-patterns, operator notes, waiver process, and no-skeleton-code enforcement.

---

## How To Do It

### Step 1: Define Anti-Patterns

**Standard anti-patterns (always block):**

1. **ANTI-GIANT-REFACTOR-001**
   - Pattern: Massive refactors without incremental validation
   - Detection: Diff size > 2000 lines in single PR
   - Mitigation: Break into smaller phases with validation gates

2. **ANTI-EDIT-OUTSIDE-SCOPE-001**
   - Pattern: Editing files outside declared file_scope
   - Detection: File touch not in allowed paths
   - Mitigation: Abort phase, require scope amendment

3. **ANTI-SKIP-TESTS-001**
   - Pattern: Skipping tests without justification
   - Detection: pytest.mark.skip without reason
   - Mitigation: Require waiver with tracking issue

4. **ANTI-HARDCODED-SECRETS-001**
   - Pattern: Hardcoded credentials or API keys
   - Detection: Secrets scanner finds matches
   - Mitigation: Use environment variables or secret store

5. **ANTI-NO-ERROR-HANDLING-001**
   - Pattern: Missing try/except in critical paths
   - Detection: Static analysis finds uncaught exceptions
   - Mitigation: Add error handling with logging

### Step 2: Notes for Operators

```json
{
  "notes_for_operators": [
    "Never modify files outside declared file_scope",
    "Always trust git/tests/filesystem over conversational summaries",
    "If tools under-deliver, repair or escalate—do not silently accept",
    "Use decision ledger for all non-trivial choices",
    "Mark [NOT_AUTOMATABLE] when truly cannot automate",
    "Bounded retries only—no infinite loops"
  ]
}
```

### Step 3: Configure Waiver Process

```json
{
  "waiver_process": {
    "allowed": true,
    "process_ref": "docs/governance/waiver_process.md",
    "waiver_criteria": [
      "Must have technical justification",
      "Must have tracking issue",
      "Must have mitigation plan",
      "Must have approval from DRI"
    ],
    "waiver_documentation": {
      "path": ".state/waivers.jsonl",
      "schema": "schemas/waiver.schema.json"
    }
  }
}
```

### Step 4: Enforce No Skeleton Code

```json
{
  "no_skeleton_code": {
    "enforcement": "MANDATORY",
    "description": "Final deliverables must be complete and runnable",
    "forbidden": [
      "TODO comments in production code",
      "pass statements in function bodies",
      "NotImplementedError without clear timeline",
      "Placeholder functions",
      "Empty test files"
    ],
    "validation_command": "python scripts/check_no_skeletons.py src/ tests/",
    "expect_exit_code": 0
  }
}
```

---

## Validation Rules

1. ≥5 anti-patterns defined
2. Each anti-pattern has: pattern_id, description, detection, mitigation
3. Notes for operators (≥5 items)
4. Waiver process configured
5. No skeleton code enforcement defined with validation command

---

## AI Agent Notes

- Anti-patterns are mechanically detected and blocked
- Waivers require justification + tracking issue
- No skeleton/placeholder code in final deliverables
- Governance is enforced by tooling, not trust



---


<a name="section-18"></a>

# SEC-018: GitHub Integration Instructions

**Schema File:** `sections/sec_018_github_integration.json`  
**Section ID:** `sec_018_github_integration`  
**Required:** No (Optional)  
**Updated:** 2026-01-28

---

## What To Do

Configure optional GitHub integration for issue/project sync (skip if not using GitHub).

---

## How To Do It

### Step 1: Decide If GitHub Integration Needed

**Enable if:**
- Project tracked in GitHub Issues
- Using GitHub Projects for planning
- Want automated phase → issue sync

**Skip if:**
- Not using GitHub
- No need for automated sync
- Tracking elsewhere

### Step 2: Configure Repository

```json
{
  "enabled": true,
  "repository": {
    "owner": "myorg",
    "name": "myrepo",
    "default_branch": "main",
    "visibility": "private"
  }
}
```

### Step 3: Configure Issue Management

```json
{
  "issue_management": {
    "mode": "one-per-phase",
    "issue_config": {
      "title_template": "[{phase_id}] {title}",
      "body_template_path": "templates/github_issue.md",
      "labels": ["phase-plan", "ai-generated"],
      "assignees": ["myusername"]
    }
  }
}
```

### Step 4: Configure Project Sync

```json
{
  "project_management": {
    "url": "https://github.com/orgs/myorg/projects/5",
    "owner": "myorg",
    "project_number": 5,
    "field_mappings": {
      "phase_id_field": "Phase ID",
      "status_field": "Status"
    },
    "status_mappings": {
      "planned": "Todo",
      "in_progress": "In Progress",
      "blocked": "Blocked",
      "done": "Done"
    }
  }
}
```

### Step 5: Configure Automation

```json
{
  "automation": {
    "allow_issue_create": true,
    "allow_issue_update": true,
    "sync_direction": "yaml->github",
    "on_phase_status_change_update_project": true
  }
}
```

---

## Validation Rules

1. If enabled=true, repository config required
2. If issue_management configured, mode must be specified
3. If project_management configured, URL and mappings required
4. Sync script command specified

---

## AI Agent Notes

- This section is OPTIONAL
- Most projects can skip (set enabled=false)
- Useful for teams using GitHub Projects
- Enables automated phase tracking in GitHub



---


<a name="section-19"></a>

# SEC-019: Operational Runbooks Instructions

**Schema File:** `sections/sec_019_runbooks.json`  
**Section ID:** `sec_019_runbooks`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Provide operational procedures for installation, execution, maintenance, rollback, and troubleshooting.

---

## How To Do It

### Step 1: Installation Runbook

**Prerequisites:**
```json
{
  "prerequisites": {
    "os": ["Windows", "Linux", "macOS"],
    "runtime": "Python >=3.11, <3.13",
    "tools": ["git >=2.40", "docker >=24 (optional)"]
  }
}
```

**Installation Steps:**
```json
{
  "steps": [
    {"step": "1. Clone repository", "command": "git clone {{REPO_URL}}"},
    {"step": "2. Create virtual environment", "command": "python -m venv .venv"},
    {"step": "3. Activate venv", "command": ".venv\\Scripts\\activate"},
    {"step": "4. Install dependencies", "command": "pip install -r requirements.txt"},
    {"step": "5. Verify", "command": "python scripts/verify_install.py", "expect_exit_code": 0}
  ]
}
```

**Troubleshooting:**
```json
{
  "troubleshooting": [
    {
      "issue": "pip install fails with dependency conflict",
      "solution": "pip install --upgrade pip && pip install -r requirements.txt --force-reinstall"
    }
  ]
}
```

### Step 2: Execution Runbook

**Development:**
```json
{
  "development": [
    {"step": "1. Generate plan", "command": "python scripts/generate_plan.py --input {{INPUT}} --output {{OUTPUT}}"},
    {"step": "2. Validate plan", "command": "python scripts/validate_plan.py {{PLAN_FILE}}", "expect_exit_code": 0},
    {"step": "3. Execute (dry-run)", "command": "python scripts/execute_plan.py {{PLAN_FILE}} --dry-run"},
    {"step": "4. Execute (real)", "command": "python scripts/execute_plan.py {{PLAN_FILE}} --execute"}
  ]
}
```

### Step 3: Maintenance Runbook

**Backup:**
```json
{
  "backup": [
    {"step": "Backup state DB", "command": "sqlite3 .state/pipeline.db '.backup .state/backups/pipeline_$(date +%Y%m%d).db'"},
    {"step": "Backup decision ledger", "command": "cp .state/decisions.jsonl .state/backups/"}
  ]
}
```

**Upgrade:**
```json
{
  "upgrade": [
    {"step": "1. Backup", "command": "python scripts/backup_all.py"},
    {"step": "2. Pull changes", "command": "git pull origin main"},
    {"step": "3. Upgrade deps", "command": "pip install -r requirements.txt --upgrade"},
    {"step": "4. Verify", "command": "python scripts/verify_upgrade.py", "expect_exit_code": 0}
  ]
}
```

### Step 4: Rollback Runbook

```json
{
  "default_procedure": [
    {"step": "1. Revert commits", "command": "git revert {{COMMIT_HASH}}"},
    {"step": "2. Rerun validations", "command": "pytest tests/ -v", "expect_exit_code": 0},
    {"step": "3. Verify rollback", "command": "python scripts/verify_rollback.py"}
  ]
}
```

### Step 5: Troubleshooting Guide

```json
{
  "common_issues": [
    {
      "issue": "Phase fails with scope violation",
      "diagnosis": "File touched outside allowed paths",
      "resolution": "Review file_scope in phase contract, amend if needed"
    },
    {
      "issue": "Circuit breaker trips",
      "diagnosis": "Oscillation detected in fix loop",
      "resolution": "Review fix attempt logs, manual intervention required"
    }
  ],
  "escalation_paths": [
    {"level": "L1", "trigger": "First failure", "action": "Invoke fix pattern"},
    {"level": "L2", "trigger": "Circuit breaker trip", "action": "Human review"},
    {"level": "L3", "trigger": "Scope violation", "action": "Plan amendment"}
  ]
}
```

---

## Validation Rules

1. Installation runbook with ≥5 steps
2. Execution runbook for development
3. Maintenance runbook with backup/restore/upgrade
4. Rollback runbook defined
5. Troubleshooting guide with ≥3 common issues

---

## AI Agent Notes

- Runbooks are for HUMANS to operate the system
- All commands must be copy-pasteable
- Include expected outcomes for verification steps
- Troubleshooting addresses common failure modes



---


<a name="section-20"></a>

# SEC-020: Final Summary + Completion Checklist Instructions

**Schema File:** `sections/sec_020_final_summary.json`  
**Section ID:** `sec_020_final_summary`  
**Required:** Yes  
**Updated:** 2026-01-28

---

## What To Do

Summarize what was delivered, determinism score, time analysis, quality gates, next steps, and completion checklist.

---

## How To Do It

### Step 1: Summarize Delivered Features

```json
{
  "what_was_delivered": {
    "description": "Phase Plan v{{VERSION}} with ALL Deterministic Enhancements",
    "features_delivered": [
      {"feature": "Self-healing rules", "details": "5 failure types with bounded retries"},
      {"feature": "Ground truth levels", "details": "L0-L5 with executable commands"},
      {"feature": "Phase contracts", "details": "{{N}} phases with machine-checkable contracts"},
      {"feature": "Validation gates", "details": "{{N}} gates with regex patterns"},
      {"feature": "Decision ledger", "details": "{{N}} decisions with rationale"}
    ]
  }
}
```

### Step 2: Report Determinism Score

```json
{
  "determinism_score": {
    "target": "0.95",
    "estimated_achieved": "0.98",
    "factors": [
      "Decision ledger: 12 decisions logged",
      "Assumption ledger: 15 assumptions tracked",
      "File scope enforcement: Hard boundaries per phase",
      "Ground truth levels: Mechanical pass/fail",
      "Parallel execution: DAG-scheduled"
    ]
  }
}
```

### Step 3: Time Analysis

```json
{
  "time_analysis": {
    "sequential_estimate_minutes": 480,
    "parallel_estimate_minutes": 180,
    "speedup_percentage": 62.5,
    "bottlenecks": [
      {"phase_or_group": "PH-003", "duration_minutes": 120, "reason": "Integration tests"}
    ]
  }
}
```

### Step 4: Quality Gates Summary

```json
{
  "quality_gates_summary": {
    "total_gates": 25,
    "passed": 0,
    "failed": 0,
    "pending": 25,
    "all_gates_have": [
      "Executable commands: YES",
      "Expected output patterns (regex): YES",
      "Evidence artifact paths: YES",
      "Pass/fail conditions: YES"
    ]
  }
}
```

### Step 5: Next Steps

```json
{
  "next_steps": [
    {"step": "Execute PH-001", "command": "python scripts/execute_phase.py PH-001"},
    {"step": "Validate assumptions", "command": "python scripts/validate_assumptions.py --impact-min=7"},
    {"step": "Monitor metrics", "command": "python scripts/monitor_metrics.py --follow"}
  ]
}
```

### Step 6: Completion Checklist

**Pre-Execution:**
```json
{
  "pre_execution": [
    {"item": "All required sections completed", "status": "DONE"},
    {"item": "All phases have phase contracts", "status": "DONE"},
    {"item": "All validations have commands + regex", "status": "DONE"},
    {"item": "Decision ledger has >=5 entries", "status": "DONE"},
    {"item": "Risk register has >=3 entries", "status": "DONE"}
  ]
}
```

**Post-Execution:**
```json
{
  "post_execution": [
    {"item": "All phases marked DONE", "status": "PENDING"},
    {"item": "All validations passed", "status": "PENDING"},
    {"item": "All artifacts present", "status": "PENDING"},
    {"item": "Global DoD criteria met", "status": "PENDING"},
    {"item": "Determinism score >= 0.95", "status": "PENDING"}
  ]
}
```

### Step 7: Known Limitations

```json
{
  "known_limitations": [
    {
      "limitation": "No GUI interface",
      "description": "CLI only, no web/graphical interface",
      "workaround": "Use CLI with --help for guidance"
    }
  ]
}
```

---

## Validation Rules

1. features_delivered has ≥5 items
2. determinism_score with target and estimated_achieved
3. time_analysis with sequential/parallel estimates
4. quality_gates_summary with total count
5. next_steps has ≥3 items
6. pre_execution checklist has ≥5 items
7. post_execution checklist has ≥5 items

---

## AI Agent Notes

- This section is the LAST to fill
- Summarizes the entire plan
- Pre-execution checklist validates plan completeness
- Post-execution checklist tracks actual execution progress
- Determinism score estimates reproducibility



---


<a name="section-21"></a>

# SEC-021: Quality Controls Catalog

## Purpose

This section defines the **Single Source of Truth (SSOT)** for proactive quality controls derived from DBG debugging topics. Instead of debugging issues after they occur, controls are enforced at planning time via the G-CTRL-001 gate.

## Key Concepts

### Control Classes

Controls are classified into categories that map to DBG topic areas:

| Class | Description | Example Controls |
|-------|-------------|------------------|
| `security` | Input validation, secrets management, access control | CTRL-SEC-001, CTRL-SEC-002 |
| `testing` | Coverage thresholds, test quality, regression prevention | CTRL-TST-001 |
| `validation` | Schema compliance, data integrity, format validation | CTRL-VAL-001 |
| `architecture` | Dependency management, module boundaries, design patterns | CTRL-ARC-001 |
| `observability` | Logging, metrics, tracing requirements | (future) |
| `deployment` | CI/CD gates, environment parity, rollback capability | (future) |
| `documentation` | API docs, changelog, decision records | (future) |

### Control Structure

Each control MUST define:

1. **control_id**: Unique identifier (format: `CTRL-{CLASS}-{SEQ}`)
2. **class**: One of the defined control classes
3. **enforcement**: Gate ID + validation command + expected output
4. **evidence_requirements**: Artifact paths using deterministic formulas
5. **severity**: `critical`, `high`, `medium`, `low`

### Enforcement Policy

The **G-CTRL-001** gate enforces control coverage:

1. For each phase in the plan, identify required controls from `control_mappings.by_phase`
2. For each required control, verify a satisfying gate exists in the plan's gates
3. Fail with specific error code if any control is unsatisfied

## Error Codes

| Code | Meaning | Remediation |
|------|---------|-------------|
| `CTRL001_MISSING_SATISFYING_GATE` | Control requires a gate but none found | Add the required gate to the plan |
| `CTRL001_MISSING_EVIDENCE_MAPPING` | Control requires evidence but path not defined | Define evidence path in gate output |
| `CTRL001_UNSATISFIED_CONTROL` | Control gate did not pass | Fix the underlying issue and re-run |
| `CTRL001_INVALID_CONTROL_ID` | Referenced control_id not in catalog | Use valid control_id from catalog |

## Adding New Controls

To add a new control:

1. Choose appropriate `control_id` following naming convention
2. Define enforcement with structured command (exe + args, not free-form string)
3. Define evidence path using formula variables: `{{PHASE}}`, `{{CONTROL_ID}}`, `{{ARTIFACT_NAME}}`
4. Add to `control_mappings.by_phase` for relevant phases
5. Add to `control_mappings.by_gate` linking to enforcement gate
6. Run `python validators/validate_sec_021.py sections/sec_021_quality_controls_catalog.json` to validate

## Validation

```bash
# Validate SEC-021 structure
python validators/validate_sec_021.py sections/sec_021_quality_controls_catalog.json

# Expected output:
# control_id_uniqueness: true
# all_required_fields_present: true
# evidence_paths_deterministic: true
# status: pass
```

## Integration Points

- **SEC-012**: G-CTRL-001 gate added to validation_gates
- **SEC-009**: Tool entries for control-required tools (bandit, semgrep, etc.)
- **SEC-010**: Task schema includes `controls` field
- **SEC-011**: Artifact schema includes `required_by_controls` field
- **SEC-016**: `control_test_mapping` links controls to test suites



---


