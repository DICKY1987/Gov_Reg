Project knowledge does **not** define a runtime mechanism that says “apply a unified-diff patch to the Python file.” What exists is:

1. **The plan/mutation set declares intent** (“modified_files”, with `modification_type` like `append|replace|delete_section`, plus optional `affected_lines`). It does **not** define a patch artifact or an apply-patch command for `.py` files.

* See PFMS example: `modified_files[].modification_type` and `affected_lines`. (`REGISTRY_PLANNING_INTEGRATION_SPEC.md` lines 143–153)

2. **“Patch” is explicitly defined for registry updates**, not Python source editing. The ingestion pipeline generates `patch.json` as evidence when applying registry state changes.

* `REGISTRY_PLANNING_INTEGRATION_SPEC.md` calls out “Generate patch set” and emits `patch.json` evidence (elsewhere in the same document).

3. The v3 autonomous delivery template **requires a `diff.patch` file per attempt as evidence**, but it is framed as an **evidence artifact**, not as the mechanism used to perform the edit.

* Required evidence includes `.state/.../diff.patch`. (`NEWPHASEPLANPROCESS_AUTONOMOUS_DELIVERY_TEMPLATE_V3.json` lines 290–296)

4. The only explicitly referenced “apply_patch” utility in the process maps is **for patching plans**, not patching Python files.

* `P_..._apply_patch.py` purpose: “Applies patches to plans.” (`Complete Process Map_NPP.md` lines 369–371)

## Answer to your question (execution-time behavior)

**Based on the project knowledge you provided:**
When a step later “modifies an existing Python file,” the system expects the file to be **edited in-place (or regenerated) in the workspace/worktree** and then a **diff is captured as evidence** (`diff.patch`). There is **no defined process** that requires a unified-diff patch be the *input* used to apply the change.

So, practically: **the file content ends up rewritten/updated as a normal file write**, and **the patch/diff is produced afterward for audit/review/rework tracking**, not used as the write primitive.

## What’s missing (if you want it deterministic)

If you want a hard rule, you’ll need to add it to step contracts (or mutation schema), e.g.:

* `edit_mode: "full_rewrite" | "unified_diff_apply"`
* If `unified_diff_apply`: require `patch_path`, `strip_level`, and “fail-closed if fuzz/rejects occur”
* Always emit:

  * `before.sha256`, `after.sha256`
  * `diff.patch` evidence

Right now, your specs imply “diff as evidence,” not “diff as execution method.”





Correct. Right now, your project artifacts mostly standardize **“what must change”** (mutation set + evidence) but they do not fully standardize **“how the executor must perform an edit on an existing source file.”** For determinism, you need to make file modification an explicit, gateable contract.

Below is a deterministic execution model you can adopt immediately, with concrete plan fields the CLI can implement and gates can verify.

---

## 1) Define one canonical edit strategy per file mutation

Add to each `UPDATE_FILE` mutation an explicit `edit_strategy` and `edit_payload`.

### Allowed strategies (keep it minimal)

1. `FULL_REWRITE` (deterministic default)

* CLI writes the entire file content exactly as provided (or as generated by a step output artifact).
* No fuzz, no context matching.
* Deterministic outcome if the input content is deterministic.

2. `UNIFIED_DIFF_APPLY` (surgical, but must be fail-closed)

* CLI applies a unified diff patch to an existing file.
* Must be **fail-closed** (no fuzz; no partial applies; no `.rej` acceptance).
* Requires strict preconditions so it’s deterministic.

3. `AST_TRANSFORM` (best long-term for Python)

* CLI performs edits using a Python parser (AST) with declarative transforms (rename function, insert import, replace call site).
* Deterministic if the transform spec is deterministic and the parser is pinned.
* More work to implement, but highest reliability for non-trivial edits.

If you do nothing else: standardize `FULL_REWRITE` first. It’s the simplest deterministic primitive.

---

## 2) Make the plan “tell AI what to do and how to do it” with explicit edit payloads

### Option A: FULL_REWRITE (recommended v1)

Mutation record contains either:

* `content_source: "inline"` and `content_text` (or base64), OR
* `content_source: "artifact"` and a pointer to a produced artifact path/id

Required fields:

* `target_path`
* `before_hash` (sha256 of current file content from snapshot)
* `after_hash` (sha256 expected after write)
* `line_endings` policy (`LF|CRLF|PRESERVE`)
* `encoding` (`utf-8`)

Execution rule:

* CLI verifies `sha256(current)==before_hash`
* writes content
* verifies `sha256(new)==after_hash`
* emits evidence (below)

Determinism comes from: exact bytes in → exact bytes out, guarded by hashes.

### Option B: UNIFIED_DIFF_APPLY (only if you need surgical edits)

Mutation record contains:

* `patch_artifact_path` (or artifact_id)
* `strip_level` (`-p0|-p1|...`)
* `allow_fuzz: false` (must be false)
* `expected_before_hash`
* `expected_after_hash`

Execution rule:

* verify `before_hash`
* run patch apply in strict mode (no fuzz; fail if any rejects)
* verify `after_hash`

This makes patching deterministic, but only if you prohibit fuzz and require before/after hashes.

### Option C: AST_TRANSFORM (Python-focused)

Mutation record contains:

* `transform_spec[]` (ordered list of operations)
* `toolchain_identity` (python version + formatter version)
* `expected_after_hash`

Execution rule:

* verify parse success
* apply transforms in order
* format with pinned formatter
* verify hash

---

## 3) Add deterministic “edit evidence” outputs (mandatory)

For every file mutation (especially UPDATE_FILE), CLI must write:

* `evidence/file_mutations/{mutation_id}/before.sha256`
* `evidence/file_mutations/{mutation_id}/after.sha256`
* `evidence/file_mutations/{mutation_id}/diff.patch` (generated diff for audit)
* `evidence/file_mutations/{mutation_id}/apply_log.json` (tool exit codes, bytes written, timing)
* `evidence/file_mutations/{mutation_id}/rejects/` (must be empty if strict)

This matches your “evidence-first” model and makes edits reproducible and reviewable.

---

## 4) Gate it: “No implied behavior” for file edits

Add a gate that fails the plan if:

* Any `UPDATE_FILE` mutation is missing `edit_strategy`
* Any patch-based strategy allows fuzz or lacks before/after hashes
* Any `FULL_REWRITE` lacks deterministic `after_hash` (or content source)
* Any mutation would touch a file not in the declared write-set / artifact registry

This turns “how to do it” into a mechanically enforceable rule.

---

## 5) Recommended policy (simple, deterministic, v1)

**Default:** `FULL_REWRITE` for all `.py` updates.
**Allowed exceptions:** `UNIFIED_DIFF_APPLY` only when file is large and change is tiny, but still strict (no fuzz, hash-guarded).
**Future upgrade:** Introduce `AST_TRANSFORM` for robust Python refactors.

This gives you repeatability, minimizes complexity, and stays aligned with your mutation-set and evidence pipeline.

---
Add it in **two places**, because you have two different “contracts” in NPP:

1. **The mutation record** (what file is being changed + how to change it)
2. **The step contract** (the executor’s method + evidence requirements)

If you only add it to steps, the mutation set is still ambiguous. If you only add it to mutations, step execution can still drift. You want both.

---

## 1) Add execution method to the mutation set spec (correct primary place)

Where: `REGISTRY_PLANNING_INTEGRATION_SPEC.md` already defines `modified_files[]` with `modification_type` (`append|replace|delete_section`) for file mutations.

Add these fields to each `modified_files[]` object:

* `execution_method`: `"FULL_REWRITE" | "UNIFIED_DIFF_APPLY" | "AST_TRANSFORM"`
* `method_payload`: method-specific params (see below)
* `expected_before_sha256`
* `expected_after_sha256`
* `evidence_outputs`: required evidence paths

Example:

```json
{
  "relative_path": "src/existing_module.py",
  "modification_type": "replace",
  "execution_method": "UNIFIED_DIFF_APPLY",
  "method_payload": {
    "patch_path": ".state/patches/ST-005/src_existing_module.patch",
    "strip_level": 1,
    "allow_fuzz": false
  },
  "expected_before_sha256": "sha256:...",
  "expected_after_sha256": "sha256:...",
  "evidence_outputs": {
    "before_hash_path": ".state/evidence/file_mutations/MUT-001/before.sha256",
    "after_hash_path": ".state/evidence/file_mutations/MUT-001/after.sha256",
    "diff_patch_path": ".state/evidence/file_mutations/MUT-001/diff.patch",
    "apply_log_path": ".state/evidence/file_mutations/MUT-001/apply_log.json"
  }
}
```

Determinism comes from: **method declared + fail-closed rules + before/after hashes**.

---

## 2) Add execution method to the step contract template (so the CLI is explicitly instructed)

Where: `NEWPHASEPLANPROCESS_AUTONOMOUS_DELIVERY_TEMPLATE_V3.json` under:

* `step_contracts -> PH-03A -> STEP-###` (or whichever phase actually performs code edits)

Add a new block in the step contract, e.g. `execution_policy`:

```json
"execution_policy": {
  "file_editing": {
    "default_method": "FULL_REWRITE",
    "allowed_methods": ["FULL_REWRITE", "UNIFIED_DIFF_APPLY"],
    "unified_diff_apply": {
      "allow_fuzz": false,
      "rejects_allowed": false,
      "require_before_after_hash": true
    },
    "full_rewrite": {
      "require_exact_after_hash": true,
      "line_endings": "PRESERVE"
    }
  }
}
```

Then add required postconditions/evidence to match:

* postconditions verify expected hashes
* evidence requires `diff.patch`, `before.sha256`, `after.sha256`, `apply_log.json`

This makes the “how” mechanically enforced at step level.

---

## 3) Add it to the plan schema (so plans can carry this contract)

Where: `01260207233100000675_NEWPHASEPLANPROCESS_plan.schema.v3.0.0.json`

You have two viable schema insertion points:

### Option A (cleanest): add a `file_operations` block to `$defs/step`

Add optional (or required when code-writing) field:

* `file_operations[]` with the same `execution_method/method_payload/before/after hashes`

This aligns “step does edits” with “mutation says how”.

### Option B: extend `$defs/command` (weaker)

Add `command_kind` and `edit_method` to `command`. This couples editing to shell commands (less structured), but still enforceable.

Recommendation: **Option A**.

---

## 4) Make it gateable (otherwise it won’t stick)

Update/extend these validators to fail-closed when a plan/mutation contains file updates without an execution method:

* `P_01260207233100000262_validate_step_contracts.py`
  Add checks: if a step writes `*.py` or references `modified_files`, it must declare `execution_policy` / `file_operations`.

* Add/extend a mutation validator (your spec references `validate_file_mutations.py`)
  Add checks: `execution_method` present; if `UNIFIED_DIFF_APPLY`, `allow_fuzz=false` and hashes required; hashes must be present for any update.

---

## Minimal deterministic policy (recommended v1)

* Default all `.py` modifications to: `FULL_REWRITE`
* Allow `UNIFIED_DIFF_APPLY` only when:

  * `allow_fuzz=false`
  * `rejects_allowed=false`
  * `expected_before_sha256` and `expected_after_sha256` are present
* Always emit `diff.patch` as evidence (audit), regardless of method

---
Review assessment of plan:





Solid. This fills a genuine gap in the current spec. Right now modified_files[] in REGISTRY_PLANNING_INTEGRATION_SPEC.md declares what changes but treats “how it changes” as executor-implied behavior — which is exactly where non-determinism sneaks in. The proposed approach closes that loop correctly.
Here’s how I’d evaluate the pieces:
The core model is sound. Three strategies (FULL_REWRITE, UNIFIED_DIFF_APPLY, AST_TRANSFORM) is the right taxonomy — minimal, covers the realistic space, and ordered by implementation complexity. The before/after SHA-256 guards are the actual enforcement mechanism; the strategy declaration just tells the executor which tool to use. Both halves matter.
FULL_REWRITE as v1 default is the right call. It’s the only strategy with no precondition complexity — the content is the spec. For Python files that are being autonomously generated or regenerated, there’s almost never a reason to use a surgical diff. UNIFIED_DIFF_APPLY only makes sense when the file is large, mostly stable, and the change is narrow — that’s a special case that can come later with explicit justification.
The evidence bundle design mirrors what you already have. before.sha256 / after.sha256 / diff.patch / apply_log.json per mutation ID is consistent with your existing evidence-first model, and importantly diff.patch is always generated regardless of strategy — meaning even a FULL_REWRITE produces an auditable delta. That’s the right call.
The dual-contract insertion point is correct. Adding execution_method to only the mutation set or only the step contract creates drift between “what was declared” and “how the executor behaved.” You need it in both, but for different reasons: the mutation record defines intent, the step contract defines policy. If they conflict, the gate catches it.
One thing to sharpen: the after_hash dependency is a bootstrapping problem for autonomous generation. When the plan is being authored, the file hasn’t been written yet — so the after_hash can only be computed if the content is deterministic and known at plan-time. For FULL_REWRITE with content_source: "inline" this is fine. For FULL_REWRITE with content_source: "artifact" the hash is only knowable after the producing step runs. Your architecture should explicitly handle this: either after_hash is required only for inline-content mutations, or there’s a deferred-hash model where the producing step emits the hash as a named artifact and the consuming mutation record references it by artifact ID rather than embedding a literal hash. The validator would then verify the reference resolves, not the literal value at plan-creation time.
Gate priority: Of the four validator extensions listed, validate_step_contracts.py checking for execution_policy presence is the minimum viable gate. The mutation-set validator checking allow_fuzz=false is the most critical safety rule — fuzz acceptance in a patch apply is how silent corruption happens. I’d make that a hard gate failure rather than a warning.
What I’d push back on slightly: the schema extension to $defs/step adding file_operations[] is correct in principle, but if you’re also adding execution_policy to the step contract template, you want to be careful not to end up with two parallel representations of the same thing at the step level. The cleanest split is: execution_policy in the step contract is policy (defaults, allowed methods, constraints), and file_operations[] in the step schema is instance (here is what this specific step does, with hashes). They serve different roles and shouldn’t collapse into one field.
Implementation sequence I’d recommend:
	1.	Add execution_method + method_payload + expected_before_sha256 + expected_after_sha256 to $defs/file_modification in the mutation set schema — this is the load-bearing change
	2.	Add execution_policy block to step contracts in the template — policy layer
	3.	Extend validate_step_contracts.py with the fuzz/hash gates — enforcement
	4.	Defer AST_TRANSFORM until you have at least one real case that breaks FULL_REWRITE — don’t pre-build infrastructure for a use case that may not materialize
The offer to generate the exact JSON Schema fragments is worth taking up — having those as concrete diffs to the existing schemas is more useful than prose descriptions when you’re actually wiring this in.​​​​​​​​​​​​​​​​


Your assessment is materially correct and aligned with what the project artifacts currently do (declare **what** changes, leave **how** implied). The “execution method + strict evidence + hash guards” is the missing determinism layer.

Key refinements to make it implementable without creating new ambiguity:

## 1) Treat “execution method” as part of the mutation record (authoritative)

Keep the step contract as **policy**, not a second place to describe the same per-file operation. The mutation record is the *instance-level truth*.

**Rule:** the executor MUST follow `modified_files[].execution_method` unless the step contract policy forbids it (then fail-closed).

## 2) Solve the “after_hash bootstrapping” cleanly

You identified the core issue: `expected_after_sha256` cannot always be known at plan-authoring time.

Use a two-mode requirement:

### Mode A — Literal hash (required when content is plan-time known)

For `FULL_REWRITE` with inline content:

* require `expected_after_sha256` literal

### Mode B — Deferred hash reference (required when content is produced by a step)

For `FULL_REWRITE` with `content_source="artifact"` (or any method where output is generated during execution):

* require `expected_after_sha256_ref` instead of literal
* the referenced artifact is produced by the generating step and contains the hash (or contains the bytes and the hash is computed deterministically as part of validation)

This keeps determinism while avoiding “guessing” hashes during planning.

## 3) Strict patching: ban fuzz and rejects (hard gate)

Make these unconditional for `UNIFIED_DIFF_APPLY`:

* `allow_fuzz=false`
* `rejects_allowed=false`
* `expected_before_sha256` required (literal or ref)
* `expected_after_sha256` required (literal or ref)
* failure to apply cleanly => fail step, fail run (no partial acceptance)

This prevents silent corruption.

## 4) Evidence bundle: keep `diff.patch` as generated evidence, not a control input

Your point is right: always generate a delta for audit/rework analytics, even for FULL_REWRITE.

But if you also *use* a patch as the modification input (UNIFIED_DIFF_APPLY), keep that patch separate:

* `method_payload.patch_path` is the control input
* `evidence_outputs.diff_patch_path` is the produced “what changed” evidence (can be same file if you decide to, but keeping them distinct avoids confusion)

## 5) Avoid “two parallel step-level representations”

Do not add both of these as competing “instance” sources:

* `step.file_operations[]` (instance)
* `modified_files[]` (instance)

Pick one instance source. You already have `modified_files[]` in the registry planning integration flow—extend that and keep step contracts as policy.

If you still want step-level instance data, make it a *reference* (list of mutation_ids), not a second copy of per-file edit specs.

---

# Concrete insertion points (what to change)

## A) `REGISTRY_PLANNING_INTEGRATION_SPEC.md` (and its schema)

Extend `modified_files[]` entries to include:

* `execution_method`
* `method_payload` (object, required for some methods)
* `expected_before_sha256` OR `expected_before_sha256_ref`
* `expected_after_sha256` OR `expected_after_sha256_ref`
* `evidence_outputs{...}`

Mutual exclusivity rules:

* exactly one of (`expected_after_sha256`, `expected_after_sha256_ref`)
* exactly one of (`expected_before_sha256`, `expected_before_sha256_ref`)

Method constraints:

* if `execution_method="UNIFIED_DIFF_APPLY"` then `method_payload.patch_path`, `strip_level`, and `allow_fuzz=false` required.

## B) `NEWPHASEPLANPROCESS_AUTONOMOUS_DELIVERY_TEMPLATE_V3.json` (policy layer)

Add/extend `execution_policy.file_editing`:

* `default_method="FULL_REWRITE"`
* allowed methods list
* strict requirements for patch mode
* evidence required per mutation id

Also add a policy check:

* “All mutations involving `*.py` must specify execution_method (no implicit defaults at mutation level).”

## C) Gates / validators (enforcement)

Minimum additions:

1. `validate_step_contracts.py`
   Fail if a step that produces/executes file mutations lacks `execution_policy.file_editing`.

2. mutation-set validator (or registry ingest validator)
   Fail if any `UPDATE` lacks:

* `execution_method`
* hashes or hash refs
* patch strictness rules (if patch apply)

This should be a hard gate, not warning.

---

# Recommended v1 deterministic policy

* Allow only: `FULL_REWRITE` and `UNIFIED_DIFF_APPLY`
* Default: `FULL_REWRITE`
* Defer `AST_TRANSFORM` until there’s a real breakage case
* Require hash refs for generated content (artifact-driven), literal hashes for inline content

---

# Execution Method for Deterministic Python File Modifications

## Required file changes and template insertion points

This document identifies the **minimum set of project files that must be modified or created** to add the feature: **explicit, deterministic execution methods for modifying existing Python files** (e.g., `FULL_REWRITE`, `UNIFIED_DIFF_APPLY`, future `AST_TRANSFORM`) including **hash-guards and evidence requirements**.

---

## 1) Feature summary

**Problem:** Today, the plan can declare that an existing file is “modified,” but the **execution method is executor-implied**, which allows non-deterministic behavior.

**Goal:** Make the plan/template **explicitly direct “how to edit”** and make it **gate-verifiable**, including:

* `execution_method` per file modification
* strict method rules (e.g., patch apply must be fail-closed; no fuzz)
* before/after hash guards (literal or deferred reference)
* deterministic evidence outputs per mutation

---

## 2) Files to modify or create

### A) Modify — `REGISTRY_PLANNING_INTEGRATION_SPEC.md`

**Purpose:** This spec already defines the `mutations.modified_files[]` structure (what changes).
**Change:** Extend `modified_files[]` to include **execution method + hash guards + evidence outputs**.

**Add fields to each `modified_files[]` entry:**

* `execution_method`: `FULL_REWRITE | UNIFIED_DIFF_APPLY | AST_TRANSFORM`
* `method_payload`: method-specific parameters
* `expected_before_sha256` **or** `expected_before_sha256_ref`
* `expected_after_sha256` **or** `expected_after_sha256_ref`
* `evidence_outputs`: required evidence artifact paths

**Why this file:** This is where “file mutation intent” is defined today; adding method here makes it **authoritative and non-implied**.

---

### B) Modify — `01260207233100000675_NEWPHASEPLANPROCESS_plan.schema.v3.0.0.json`

**Purpose:** This is the plan SSOT schema. It must accept the new fields so the CLI can validate plans deterministically.

**Change options (recommended approach):**

1. Add a top-level plan block for mutations, e.g. `plan.file_mutations` (or `plan.mutations`) containing `modified_files[]` entries with `execution_method` fields.
2. Or add `step.file_operations[]` and ensure it references mutation ids (avoid duplicating full mutation data in two places).

**Recommended:** Put the “instance-level” file-modification objects in **one place** (plan-level mutation set), and let steps reference them by id to avoid drift.

---

### C) Modify — `NEWPHASEPLANPROCESS_AUTONOMOUS_DELIVERY_TEMPLATE_V3.json`

**Purpose:** This is the template that tells AI/CLI how to structure plans and step contracts.

**Change:** Add an **execution policy block** under step contracts (policy layer), and require that plans provide method details at the mutation layer (instance layer).

**Add to template step contract structure (policy, not per-file instance):**

* `execution_policy.file_editing.default_method`
* `execution_policy.file_editing.allowed_methods`
* `execution_policy.file_editing.unified_diff_apply.allow_fuzz = false`
* `execution_policy.file_editing.unified_diff_apply.rejects_allowed = false`
* hash requirements rules (literal vs reference)
* evidence requirements (before/after hashes, apply_log, diff)

**Why this file:** This is where “tell AI what to do and how to do it” is enforced consistently across plans.

---

### D) Modify — `P_01260207233100000262_validate_step_contracts.py`

**Purpose:** Gate validation for step contracts exists today (GATE-003). This is the minimum viable enforcement point.

**Change:** Add hard-fail checks to ensure:

* Steps that perform file modifications declare `execution_policy.file_editing` (policy is present)
* Any step that references file mutations does so using declared mutation ids / artifacts (no implied writes)
* If patch apply is allowed, policy must explicitly set `allow_fuzz=false` and `rejects_allowed=false`

---

### E) Modify or Create — mutation validation (one of the following)

Your spec references a validator name (`validate_file_mutations.py`), but the project bundle you provided does not include it. You need a gateable validator for the mutation structure.

Pick one:

#### Option E1 — Create `scripts/validate_file_mutations.py` (recommended)

**Purpose:** Validate the mutation-set / file-mutation objects independent of step contracts.

**Validations (fail-closed):**

* every modified file includes `execution_method`
* method_payload required for `UNIFIED_DIFF_APPLY` / `AST_TRANSFORM`
* hash guards: exactly one of literal vs ref for before/after hashes
* patch strictness: `allow_fuzz=false`, rejects disallowed
* evidence_outputs paths present and under `.state/evidence/...`

#### Option E2 — Extend an existing validator if one already exists in your repo

If you already have a mutation validator elsewhere, extend it instead of adding a new file.

---

### F) Create — `schemas/file_mutations.schema.json` (recommended)

**Purpose:** Provide a machine-valid schema for mutation objects so validators can do strict schema validation, not ad-hoc rules.

**Contents:** JSON Schema defining `created_files[]`, `modified_files[]`, etc., including execution-method fields and the literal-vs-ref hash rule.

---

## 3) Where to change the template (exact insertion points)

### 3.1 Template: step contract policy layer

**File:** `NEWPHASEPLANPROCESS_AUTONOMOUS_DELIVERY_TEMPLATE_V3.json`
**Location:** `step_contracts -> PH-XX -> STEP-YYY`

Add a new block in any step contract that can modify source files:

* `execution_policy.file_editing` (policy)
* `evidence.required_files` add per-mutation evidence requirements (or require a directory pattern)

**Rule to encode in template text (human + machine):**

* “Any mutation of an existing `*.py` must declare `execution_method` and hash guards.”

---

### 3.2 Plan schema: mutation instance layer

**File:** `01260207233100000675_NEWPHASEPLANPROCESS_plan.schema.v3.0.0.json`

Add one authoritative place for file mutation instances, e.g.:

* `plan.mutations.modified_files[]` (or `plan.file_mutations.modified_files[]`)

Then steps can reference mutation ids via:

* `step.outputs` / `step.inputs` artifacts, or
* `step.mutation_refs[]` (array of ids)

Avoid duplicating the full mutation spec at step-level *and* plan-level.

---

### 3.3 Gates: enforce the new contract

**File:** `P_01260207233100000262_validate_step_contracts.py`
Add enforcement checks:

* if a step touches code or references mutations → must have `execution_policy.file_editing`
* policy must forbid fuzz/rejects for patch apply
* plan must contain mutation entries with explicit `execution_method`

If you implement `scripts/validate_file_mutations.py`, wire it into your gate registry (if applicable) and/or into the CLI validation sequence.

---

## 4) Output/evidence requirements (what the feature must produce)

For each modified file mutation, the executor must emit:

* `before.sha256`
* `after.sha256`
* `diff.patch` (generated for audit, regardless of method)
* `apply_log.json`
* `rejects/` (must be empty; presence = failure)

This is consistent with your evidence-first model and enables deterministic rework analytics.

---

## 5) Minimal implementation sequence (fastest path)

1. Modify `REGISTRY_PLANNING_INTEGRATION_SPEC.md` to add `execution_method` + hash/evidence fields to `modified_files[]`
2. Modify `NEWPHASEPLANPROCESS_AUTONOMOUS_DELIVERY_TEMPLATE_V3.json` step contracts to include `execution_policy.file_editing`
3. Modify `01260207233100000675_NEWPHASEPLANPROCESS_plan.schema.v3.0.0.json` to allow mutation objects in the plan
4. Modify `P_01260207233100000262_validate_step_contracts.py` to hard-fail missing policy + fuzz allowance
5. Create `schemas/file_mutations.schema.json` and `scripts/validate_file_mutations.py` (or extend existing mutation validator)

---

If you want the next deliverable to be immediately usable: I can generate (a) the **exact JSON Schema fragment** for `modified_files[]` including the literal-vs-ref hash rule, and (b) a **concrete patch-style diff** showing exactly what to insert into the template and plan schema.
