# DOC_ID: DOC-SCRIPT-1043
# AI CLI Provenance Solution - Setup Guide

## Quick Setup

### 1. Create Virtual Environment
```powershell
cd C:\Users\richg\ALL_AI\AI_CLI_PROVENANCE_SOLUTION
python -m venv .venv
.venv\Scripts\Activate.ps1
```

Or use the setup script:
```powershell
.\setup_environment.ps1
```

### 2. Install Dependencies
```powershell
pip install -r requirements.txt
```

### 3. Verify Installation
```powershell
python -c "import yaml; print('PyYAML installed')"
pytest --version
```

### 4. Validate Evidence Schema
```powershell
python validate_evidence_schema.py --verbose
```

### 5. Run Tests (once implemented)
```powershell
pytest tests/ -v --cov
```

### 6. Check Log Availability (once implemented)
```powershell
python validate_log_availability.py
```

## Development Setup

### Enable Git (if not already)
```powershell
git init
git add .gitignore
git commit -m "Initial commit: AI CLI Provenance Solution"
```

### Pre-commit Hook (optional)
```powershell
# Run tests before commit
pytest 4_TESTS/ --exitfirst
```

## Next Steps

1. Complete Phase 0: Evidence schema extension
2. Implement Phase 2: Collectors and parsers
3. Run full test suite
4. Deploy to RUNTIME/identification_criteria/

**Last Updated**: 2026-01-04
