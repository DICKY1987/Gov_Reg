# Rollback Procedure

If Phase 2 fails or tests break:

## Full Rollback (abort implementation)
1. git checkout main
2. git reset --hard pre-dtoa-implementation
3. git clean -fdx src/registry_transition tests/conftest.py
4. Restore modified files:
   - git checkout HEAD -- transition_contract.bundle.yaml
   - git checkout HEAD -- transition_vectors.yaml
   - git checkout HEAD -- scripts/calc_determinism_score.py
   - git checkout HEAD -- scripts/run_batch_vectors.py
5. Run existing tests: pytest tests/
6. Verify: All 21 existing tests pass

## Partial Rollback (keep completed phases)
Check which phases completed:
   ls .state/phase_markers/

Only rollback incomplete phase work:
   - PH-00: rm -rf src/registry_transition/__init__.py
   - PH-01: git checkout HEAD -- transition_contract.bundle.yaml
   - PH-02: rm -rf src/registry_transition/{lifecycle_state,state_machine,identity_resolver,field_precedence,registry_writer,gates}.py
   - PH-03: git checkout HEAD -- transition_vectors.yaml
   - PH-04: git checkout HEAD -- scripts/calc_determinism_score.py

Generated: $(Get-Date -Format 'yyyy-MM-ddTHH:mm:ss.fffZ')
