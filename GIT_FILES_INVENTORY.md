# Git-Related Files Inventory
Generated: 2026-04-07

---

## 1. Core Git Configuration

### `.git/config`
**Purpose:** Local repository configuration.
- Remote `origin` → `https://github.com/DICKY1987/Gov_Reg.git`
- Tracks two branches: `master` and `feature/registry-automation-remediation`
- No custom hooks, aliases, or fetch overrides.

---

## 2. `.gitignore` Files

| File | Purpose |
|------|---------|
| `.gitignore` (root) | Excludes secrets (`open api key and GitHub Token.txt`, `*.env`), Python cache, lock files, OS files |
| `01260207201000001250_REGISTRY/REGISTRY_AUTOMATION/.gitignore` | Not readable (deleted on current branch — staged for removal) |
| `RUNTIME/environment_manager/SUB_AIM/aim_tools/src/aim/ai-logs-analyzer/.gitignore` | Excludes test output, log data, analysis exports, SQLite DBs; keeps scripts/configs |
| `01260207201000001250_REGISTRY/01260207201000001313_capability_mapping_system/01999000042260125146_AI_CLI_PROVENANCE_SOLUTION/.gitignore` | Excludes SQLite DBs, Python cache, test artifacts, virtual environments |
| `01260207201000001133_backups/REGISTRY_pre_unified_cleanup_20260222_200819/.pytest_cache/.gitignore` | Auto-generated by pytest; excludes pytest cache directory |
| `.pytest_cache/.gitignore` | Auto-generated by pytest; excludes pytest cache directory |

---

## 3. Git Hooks

**Location:** `.git/hooks/`
**Status:** All hooks are `.sample` files only — **none are active.**

Samples present (disabled): `applypatch-msg`, `commit-msg`, `fsmonitor-watchman`, `post-update`, `pre-applypatch`, `pre-commit`, `pre-merge-commit`, `pre-push`, `pre-rebase`, `pre-receive`, `prepare-commit-msg`, `push-to-checkout`, `sendemail-validate`, `update`

---

## 4. GitHub Actions Workflows

All workflows live under `RUNTIME/integrations/github/SUB_GITHUB/.github/workflows/` unless noted. These are **not active in this repo** — they are stored here as part of the GitHub integration module and would only run if deployed to GitHub.

### Scheduled (Cron) Workflows — would run automatically on GitHub

| File | Name | Schedule | Purpose |
|------|------|----------|---------|
| `state-file-cleanup.yml` | State File Cleanup | Daily 2 AM UTC | Removes stale state files from the repo |
| `scheduled-orchestrator.yml` | Scheduled Orchestrator | Daily 2 AM UTC | Runs the phase orchestrator on a timer |
| `pattern-automation.yml` | Pattern Automation | Every 6 hrs / Weekly / Monthly | Pattern detection, performance reports, pattern cleanup |
| `doc-id-validation.yml` | DOC_ID Validation | Daily 2 AM UTC | Validates document IDs on a schedule (also triggers on push/PR) |
| `MINI_PIPE/.github/workflows/acms-pipeline.yml` | ACMS Daily Gap Analysis | Daily 2 AM UTC | Gap analysis for the ACMS mini-pipeline |

### Push-Triggered Workflows

| File | Name | Trigger | Purpose |
|------|------|---------|---------|
| `changelog.yml` | Generate Changelog | push (tag) | Auto-generates changelog on new tags |
| `registry_integrity.yml` | Registry Integrity Check | push + PR | Validates registry file integrity |
| `splinter_phase_sync.yml` | Sync SPLINTER Phase Plan | push | Syncs phase plan data to GitHub Projects |
| `quality-gates.yml` | Quality Gates | push + PR | Enforces quality standards before merge |
| `module_id_validation.yml` | Module ID Validation | push + PR | Validates module ID format/uniqueness |
| `path_standards.yml` | Path Standards Check | push + PR | Enforces file path naming conventions |
| `incomplete-scanner.yml` | Block Incomplete Implementations | push + PR | Blocks stub/incomplete code from merging |
| `glossary-validation.yml` | Glossary Validation | push + PR | Validates glossary entries and applies patches |
| `documentation.yml` | Documentation Validation | push + PR | Validates doc structure and links |
| `validate-patterns.yml` | Pattern Validation CI | push + PR | Validates code/doc patterns |
| `deploy-staging.yml` | Deploy to Staging | push | Deploys to staging environment |
| `deploy-production.yml` | Deploy to Production | (manual/other) | Deploys to production |
| `ci.yml` (SUB_GITHUB) | CI Pipeline | push + PR | Main CI: tests on Ubuntu + Windows, Python 3.12 |
| `doc-id-validation.yml` | DOC_ID Validation | push + PR + schedule | Validates DOC_ID fields across all files |
| `MINI_PIPE/.github/workflows/ci.yml` | CI Pipeline | push + PR | CI for the MINI_PIPE sub-module |
| `MINI_PIPE/.github/workflows/lint.yml` | Lint | push + PR | Linting for MINI_PIPE |

### PR-Only Workflows

| File | Name | Purpose |
|------|------|---------|
| `docs-guard.yml` | Docs Guard | Blocks PRs that modify docs without approval |
| `milestone_completion.yml` | Milestone Completion Sync | Syncs milestone status to GitHub Projects on PR |
| `project_item_sync.yml` | Project Item Sync | Syncs PR to GitHub Projects board |
| `branch-handler.yml` | Branch Creation Handler | Handles branch creation events on PR |

### Sync-Pipeline Workflows (`sync-pipeline/.github/workflows/`)

| File | Name | Trigger | Purpose |
|------|------|---------|---------|
| `triggerA-remote-to-local.yml` | TriggerA Remote→Local | push | Syncs changes from remote GitHub to local (self-hosted runner) |
| `remote-sync.yml` | Remote Sync Handler | push | Handles remote sync operations |

### Dependency Management

| File | Purpose |
|------|---------|
| `dependabot.yml` | Weekly Dependabot updates — pip (Mondays 9 AM) and GitHub Actions deps; groups minor/patch; separates major version bumps |

---

## 5. Auto-Sync Infrastructure (PowerShell)

**Location:** `RUNTIME/integrations/github/SUB_GITHUB/.github/infra/sync/`

These scripts are **not running** — they must be manually installed/started.

| File | Purpose |
|------|---------|
| `Install-GitAutoSync.ps1` | Installs a Windows Service that provides zero-touch git sync (commit + push loop) |
| `Start-AutoSync.ps1` | Smart launcher — checks for an already-running sync job via lock file before starting; targets a hardcoded repo path (`C:\Users\richg\ALL_AI\...`). Runs `GitAutoSync.ps1` with **CommitInterval=30s, SyncInterval=60s** |
| `Watch-SyncStatus.ps1` | Real-time monitor for sync status; reads `.sync-log.txt` and lock file, refreshes every 5s |

**Key detail:** `Start-AutoSync.ps1` hardcodes the path to a *different* repo (`ALL_AI\Complete AI Development Pipeline...`), not `Gov_Reg`. It is not currently running (no lock file, no scheduled task found).

---

## 6. Workstream & GitHub Integration Scripts

**Location:** `RUNTIME/integrations/github/SUB_GITHUB/.github/workstream/`

These are Python/PowerShell scripts that coordinate multi-branch workstream work via git. They are invoked manually or from CI.

| File | Purpose |
|------|---------|
| `check_workstream_status.sh` / `.ps1` | Read-only: reports active workstream branches, worktrees, and key file implementation status via `git branch`, `git worktree list`, `git show` |
| `sync_workstreams_to_github.py` | Syncs local workstream branch state to GitHub Projects |
| `phase_plan_to_workstream.py` | Converts a phase plan document into workstream branch definitions |
| `spec_to_workstream.ps1` | Converts a spec file into a workstream structure |
| `multi_agent_workstream_coordinator.py` | Coordinates multiple AI agents across workstream branches |
| `workstream.py` / `workstream_dao.py` | Core workstream data model and data access layer |
| `run_workstream.py` / `simple_workstream_executor.py` / `uet_execute_workstreams.py` | Workstream execution engines |
| `uet_workstream_loader.py` | Loads workstream definitions for execution |
| `track_workstream_status.py` | Tracks and persists workstream progress |
| `_003_create_workstreams.py` | Batch-creates workstream branches |
| `validate_workstreams_authoring.py` | Validates workstream definition correctness |
| `multi_workstream_doc_suite_gen_001_executor.ps1` | Generates doc suites across multiple workstreams |
| `test_*.py` / `test_*.ps1` | Unit/integration tests for workstream components |

### GitHub Integration Scripts (`github_integration_v2/scripts/`)

| File | Purpose |
|------|---------|
| `gh_epic_sync.py` | Syncs epics/milestones to GitHub Projects |
| `gh_issue_update.py` | Updates GitHub Issues from local state |
| `splinter_sync_phase_to_github.py` | Syncs SPLINTER phase plan to GitHub |
| `milestone_completion_sync.py` | Marks milestones complete in GitHub |
| `project_item_sync.py` | Syncs project items to GitHub Projects board |

---

## 7. Infra / CI Support Files

| File | Purpose |
|------|---------|
| `SUB_GITHUB/.github/infra/ci/workflows/ci.yml` | CI definition for the integration module (runs pytest on Ubuntu + Windows) |
| `SUB_GITHUB/.github/infra/ci/pytest.ini` | Pytest configuration for CI |
| `SUB_GITHUB/.github/infra/ci/sandbox_repos/sandbox_python/` | Minimal Python sandbox repo used for CI testing |
| `SUB_GITHUB/.github/infra/.ai-module-manifest` | AI module manifest for the GitHub integration module |
| `SUB_GITHUB/.github/shared/github_client.py` | Shared GitHub API client used by sync scripts |
| `SUB_GITHUB/.github/tree_sitter/` | Tree-sitter parsers (Python, JS, TS) — used by workflows to parse code for validation |

---

## Summary

| Category | Count | Currently Active? |
|----------|-------|-----------------|
| Git config files | 1 | Yes |
| .gitignore files | 6 | Yes |
| Git hooks | 14 (all samples) | **No** |
| GitHub Actions workflows | 23 | Only if pushed to GitHub |
| Auto-sync PowerShell scripts | 3 | **No** (not installed/running) |
| Workstream/integration scripts | ~25 | Manual/CI only |

**Nothing in this repo runs git operations automatically on this machine.** All automation is either GitHub Actions (server-side, only active on GitHub.com) or PowerShell scripts that must be manually started.
